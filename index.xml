<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:webfeeds="http://webfeeds.org/rss/1.0" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>Charles&#39; Personal Development Notes</title>
    <link>https://devcharl.github.io/</link>
    <description>Recent content on Charles&#39; Personal Development Notes</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>zh-cn</language>
    <lastBuildDate>Sat, 11 May 2024 16:03:16 +0800</lastBuildDate>
    
    <atom:link href="https://devcharl.github.io/index.xml" rel="self" type="application/rss+xml" />
    
    
    <item>
      <title>Git 笔记总结</title>
      <link>https://devcharl.github.io/2024/05/11/git-notes-summary/</link>
      <pubDate>Sat, 11 May 2024 16:03:16 +0800</pubDate>
      
      <guid>https://devcharl.github.io/2024/05/11/git-notes-summary/</guid>
      <description>一、git 客户端（本地仓库）的一些操作 1. 设置账户（需要和 GitHub 账户设置一致） git config --global user.name xxx git config --global user.email xxx@foxmail.com 2. 查看设置 git config --list 输出示例： user.name=xxx user.email=xxx@foxmail.com 3. 创建 git 本地仓库 git</description>
      <content:encoded><![CDATA[<h2 id="一git-客户端本地仓库的一些操作">一、git 客户端（本地仓库）的一些操作</h2>
<h3 id="1-设置账户需要和-github-账户设置一致">1. 设置账户（需要和 GitHub 账户设置一致）</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git config --global user.name xxx
</span></span><span class="line"><span class="cl">git config --global user.email xxx@foxmail.com
</span></span></code></pre></div><h3 id="2-查看设置">2. 查看设置</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git config --list
</span></span></code></pre></div><p>输出示例：</p>
<pre tabindex="0"><code>user.name=xxx
user.email=xxx@foxmail.com
</code></pre><h3 id="3-创建-git-本地仓库">3. 创建 git 本地仓库</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git init
</span></span></code></pre></div><p>此时会出现提示：<code>Initialized empty Git repository in d://com/liu/.git</code></p>
<h3 id="4-查看-git-状态">4. 查看 git 状态</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git status
</span></span></code></pre></div><p>一般来说会显示需要提交的文件（uncommitted）和未追踪的文件（untracked）：</p>
<ul>
<li><strong>uncommitted</strong>：已有的，刚被修改尚未提交的</li>
<li><strong>untracked</strong>：原先没有的，新建的</li>
</ul>
<h3 id="5-添加-git-文件到暂存区需要和版本库区分">5. 添加 git 文件到暂存区（需要和版本库区分）</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git add &lt;name&gt;
</span></span></code></pre></div><h3 id="6-git-提交文件">6. git 提交文件</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git commit -m <span class="s2">&#34;add a function in test.java&#34;</span>
</span></span></code></pre></div><p><code>-m</code> 表示注释，为提交时的说明，必须要有！</p>
<blockquote>
<p><code>git commit --amend</code> 可以继续修改本次 commit message</p>
</blockquote>
<h3 id="7-git-删除文件夹">7. git 删除文件（夹）</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git rm test.txt        <span class="c1"># 删除文件</span>
</span></span><span class="line"><span class="cl">git rm -r filebook     <span class="c1"># 删除文件夹</span>
</span></span></code></pre></div><p><code>git rm</code> 和直接删除的区别在于：<code>git rm</code> 会将此文件的操作记录删除，而直接删除仅仅是删除了物理文件，没有删除和此文件相关的记录。<code>git rm</code> 后会在版本库产生区别（有操作日志），而直接删除没有。</p>
<p>可以用下面两种操作在版本库中删除文件：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git rm test.txt
</span></span><span class="line"><span class="cl">git commit -m <span class="s1">&#39;delete a file&#39;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">rm test.txt
</span></span><span class="line"><span class="cl">git commit -am <span class="s1">&#39;delete a file&#39;</span>
</span></span></code></pre></div><p>注意：命令 <code>git rm</code> 用于删除一个文件。如果一个文件已经被提交到版本库，那么你永远不用担心误删，但是要小心，你只能恢复文件到最新版本，你会丢失最近一次提交后你修改的内容。</p>
<h3 id="8-git-操作日志">8. git 操作日志</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git log --decorate --graph --oneline --all   <span class="c1"># 显示当前及之前的版本号</span>
</span></span><span class="line"><span class="cl">git log --pretty<span class="o">=</span>oneline                     <span class="c1"># 将版本历史显示为一行，历史版本号全部显示</span>
</span></span><span class="line"><span class="cl">git log --pretty<span class="o">=</span>oneline --abbrev-commit     <span class="c1"># 将版本历史显示为一行，历史版本号部分显示</span>
</span></span><span class="line"><span class="cl">git log --graph                              <span class="c1"># 查看分支合并图</span>
</span></span></code></pre></div><h3 id="9-版本回退">9. 版本回退</h3>
<p>执行版本退回后，本地工作区的内容会自动和回退到的版本库版本的内容保持同步：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git reset --hard HEAD^              <span class="c1"># 回退到上一个版本</span>
</span></span><span class="line"><span class="cl">git reset --hard HEAD^^             <span class="c1"># 回退到上上个版本，以此类推，一次提交即为一个版本</span>
</span></span><span class="line"><span class="cl">git reset --hard e9efa77            <span class="c1"># 回退到 e9efa77 版本</span>
</span></span></code></pre></div><h3 id="10-git-还原操作">10. git 还原操作</h3>
<p>丢弃工作区的操作，但不会丢失暂存区的操作（<code>add</code> 操作能将更改添加到暂存区），实际上就是用版本库里的版本替换工作区的版本，无论工作区是修改还是删除，都可以「一键还原」：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git checkout -- readme.txt
</span></span></code></pre></div><h3 id="11-git-暂存区撤销操作">11. git 暂存区撤销操作</h3>
<p>工作区修改了文件，而且执行了 <code>add</code>，但还没执行 <code>commit</code>，暂存区还是可以撤销的：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git reset HEAD readme.txt
</span></span></code></pre></div><p>备注：<code>git reset</code> 命令既可以回退版本，也可以把暂存区的修改回退到工作区。当我们用 <code>HEAD</code> 时，表示最新的版本。</p>
<h2 id="二与-githubgitee-协同使用git-代码托管服务器">二、与 GitHub/Gitee 协同使用（git 代码托管服务器）</h2>
<p>和 GitHub 相比，码云（Gitee）也提供免费的 Git 仓库。此外，还集成了代码质量检测、项目演示等功能。对于团队协作开发，码云还提供了项目管理、代码托管、文档管理的服务，5 人以下小团队免费。</p>
<h3 id="1-配置远程仓库免密登录">1. 配置远程仓库免密登录</h3>
<p>（1）在用户主目录下，看看有没有 <code>.ssh</code> 目录，如果有，再看看这个目录下有没有 <code>id_rsa</code> 和 <code>id_rsa.pub</code> 这两个文件，如果已经有了，可直接跳到下一步。如果没有，打开 Shell（Windows 下打开 Git Bash），创建 SSH Key：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">ssh-keygen -t rsa -C <span class="s2">&#34;xxx@foxmail.com&#34;</span>
</span></span></code></pre></div><p>备注：一路回车，执行生成 <code>id_rsa</code> 私钥和 <code>id_rsa.pub</code> 公钥，Windows 用户在 git bash 中输入上述指令。</p>
<p>（2）获得 key 的内容，复制下来，添加到 GitHub 的 SSH key 中：</p>
<ul>
<li>Windows 位置：<code>C:\Users\用户名\.ssh\id_rsa.pub</code></li>
<li>Linux 位置：<code>cat ~/.ssh/id_rsa.pub</code></li>
</ul>
<p>（3）验证 key，根据提示输入 yes，添加为信任主机：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">ssh -T git@github.com
</span></span><span class="line"><span class="cl"><span class="c1"># 或者</span>
</span></span><span class="line"><span class="cl">ssh -T git@git.oschina.net
</span></span></code></pre></div><h3 id="2-添加远程仓库">2. 添加远程仓库</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git remote add origin https://github.com/xxx/LearnGit.git   <span class="c1"># https 方式</span>
</span></span><span class="line"><span class="cl">git remote add origin git@github.com:xxx/LearnGit.git        <span class="c1"># ssh 方式</span>
</span></span></code></pre></div><p>此处可以为 https 地址也可以是 ssh 地址，<code>origin</code> 为设置的远程仓库的别名，强烈建议使用 ssh 方式，因为 https 方式每次都要输入用户名和密码。</p>
<p>如果需要修改传输协议：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git remote rm &lt;远程主机名&gt;                              <span class="c1"># 删除远程仓库</span>
</span></span><span class="line"><span class="cl">git remote add &lt;远程主机名&gt; &lt;仓库地址&gt;                  <span class="c1"># 设置传输方式和目标远程仓库</span>
</span></span><span class="line"><span class="cl">git push -u &lt;远程主机名&gt; &lt;本地分支名&gt;
</span></span></code></pre></div><h4 id="码云的添加远程仓库方法">码云的添加远程仓库方法</h4>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git remote add origin git@gitee.com:xxx/LearnGit.git
</span></span></code></pre></div><p>如果 <code>git remote add</code> 失败，并报错：<code>fatal: remote origin already exists.</code></p>
<p>说明本地库已经关联了一个名叫 <code>origin</code> 的远程库，此时，可以先用 <code>git remote -v</code> 查看远程库信息：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">origin  git@github.com:xxx/LearnGit.git <span class="o">(</span>fetch<span class="o">)</span>
</span></span><span class="line"><span class="cl">origin  git@github.com:xxx/LearnGit.git <span class="o">(</span>push<span class="o">)</span>
</span></span></code></pre></div><p>表示本地库已经关联了 GitHub 上的 <code>origin</code> 远程库，需要先删除已有的 GitHub 库：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git remote remove origin
</span></span></code></pre></div><p>再关联码云的远程库（注意路径中需要填写正确的用户名）：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git remote add gitee git@gitee.com:xxx/LearnGit.git
</span></span></code></pre></div><p>因为 git 本身是分布式版本控制系统，可以同步到另外一个远程库，当然也可以同步到另外两个远程库，所以一个本地库可以既关联 GitHub，又关联码云！</p>
<p>使用多个远程库时，要注意 git 给远程库起的默认名称是 <code>origin</code>，如果有多个远程库，我们需要用不同的名称来标识不同的远程库。仍然以 learngit 本地库为例，先删除已关联的名为 <code>origin</code> 的远程库：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git remote rm origin
</span></span><span class="line"><span class="cl">git remote add github git@github.com:xxx/LearnGit.git
</span></span><span class="line"><span class="cl">git remote add gitee git@gitee.com:xxx/LearnGit.git
</span></span></code></pre></div><p>注意，远程库的名称分别叫 <code>github</code> 和 <code>gitee</code>，不叫 <code>origin</code> 了。</p>
<p>现在，我们用 <code>git remote -v</code> 查看远程库信息，可以看到两个远程库：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">gitee   git@gitee.com:xxx/LearnGit.git <span class="o">(</span>fetch<span class="o">)</span>
</span></span><span class="line"><span class="cl">gitee   git@gitee.com:xxx/LearnGit.git <span class="o">(</span>push<span class="o">)</span>
</span></span><span class="line"><span class="cl">github  git@github.com:xxx/LearnGit.git <span class="o">(</span>fetch<span class="o">)</span>
</span></span><span class="line"><span class="cl">github  git@github.com:xxx/LearnGit.git <span class="o">(</span>push<span class="o">)</span>
</span></span></code></pre></div><p>如果要推送到 GitHub，使用命令：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git push github master
</span></span></code></pre></div><p>如果要推送到码云，使用命令：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git push gitee master
</span></span></code></pre></div><p>这样一来，本地库就可以同时与多个远程库互相同步。</p>
<h3 id="3-查看远程仓库及传输协议">3. 查看远程仓库及传输协议</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git remote
</span></span><span class="line"><span class="cl">git remote -v    <span class="c1"># 查看名称和详细地址</span>
</span></span></code></pre></div><h3 id="4-删除远程仓库">4. 删除远程仓库</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git remote remove &lt;远程主机名&gt;
</span></span></code></pre></div><h3 id="5-推送本地分支到远程仓库">5. 推送本地分支到远程仓库</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git push &lt;远程主机名&gt; &lt;本地分支名&gt;:&lt;远程分支名&gt;
</span></span></code></pre></div><p>如果省略远程分支名，则表示将本地分支推送与之存在「追踪关系」的远程分支（通常两者同名），如果该远程分支不存在，则会被新建。</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git push origin &lt;本地分支名&gt;
</span></span><span class="line"><span class="cl">git push origin master
</span></span></code></pre></div><p>如果当前分支与多个主机存在追踪关系，则可以使用 <code>-u</code> 选项指定一个默认主机，这样以后就可以不加任何参数使用 <code>git push</code>：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git push -u &lt;远程主机名&gt; &lt;本地分支名&gt;
</span></span><span class="line"><span class="cl"><span class="c1"># 例如：</span>
</span></span><span class="line"><span class="cl">git push -u origin master
</span></span></code></pre></div><h3 id="6-将远程仓库克隆为本地仓库">6. 将远程仓库克隆为本地仓库</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git clone git@github.com:xxx/LearnGit.git
</span></span></code></pre></div><p>注意：</p>
<ol>
<li>不能使用别名</li>
<li>默认情况下，从远程 clone 到本地的库只能看到 <code>master</code> 分支，如果要将远程的分支同步到本地：</li>
</ol>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git checkout -b &lt;本地分支名&gt; &lt;远程主机名&gt;/&lt;远程分支名&gt;
</span></span></code></pre></div><p>前提是远程 <code>&lt;远程主机名&gt;</code> 必须存在名为 <code>&lt;远程分支名&gt;</code> 的分支，而且 <code>&lt;本地分支名&gt;</code> 和 <code>&lt;远程分支名&gt;</code> 最好一致。</p>
<h3 id="7-本地仓库更新">7. 本地仓库更新</h3>
<p>将远程存储库中的更改合并到当前分支中。在默认模式下，<code>git pull</code> 是 <code>git fetch</code> 后跟 <code>git merge FETCH_HEAD</code> 的缩写。更准确地说，<code>git pull</code> 使用给定的参数运行 <code>git fetch</code>，并调用 <code>git merge</code> 将检索到的分支头合并到当前分支中。使用 <code>--rebase</code>，它运行 <code>git rebase</code> 而不是 <code>git merge</code>。</p>
<p>以下是一些示例：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git pull &lt;远程主机名&gt; &lt;远程分支名&gt;:&lt;本地分支名&gt;
</span></span></code></pre></div><p>比如，要取回 <code>origin</code> 主机的 <code>next</code> 分支，与本地的 <code>master</code> 分支合并，需要写成：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git pull origin next:master
</span></span></code></pre></div><p>如果远程分支（<code>next</code>）要与当前分支合并，则冒号后面的部分可以省略：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git pull origin next
</span></span></code></pre></div><p>上面命令表示，取回 <code>origin/next</code> 分支，再与当前分支合并。实质上，这等同于先做 <code>git fetch</code>，再执行 <code>git merge</code>：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git fetch origin
</span></span><span class="line"><span class="cl">git merge origin/next
</span></span></code></pre></div><p>在某些场合，Git 会自动在本地分支与远程分支之间，建立一种追踪关系（tracking）。比如，在 <code>git clone</code> 的时候，所有本地分支默认与远程主机的同名分支，建立追踪关系，也就是说，本地的 <code>master</code> 分支自动「追踪」<code>origin/master</code> 分支。Git 也允许手动建立追踪关系：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git branch --set-upstream-to<span class="o">=</span>&lt;远程主机名&gt;/&lt;远程分支名&gt; &lt;本地分支名&gt;
</span></span></code></pre></div><p>比如 <code>git branch --set-upstream-to=origin/next master</code>，指定 <code>master</code> 分支追踪 <code>origin/next</code> 分支。</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git pull origin    <span class="c1"># 本地当前分支自动与对应的 origin 主机「追踪分支」进行合并</span>
</span></span><span class="line"><span class="cl">git pull           <span class="c1"># 当前分支自动与唯一一个追踪分支进行合并</span>
</span></span><span class="line"><span class="cl">git pull --rebase &lt;远程主机名&gt; &lt;远程分支名&gt;:&lt;本地分支名&gt;
</span></span></code></pre></div><p>如果合并需要采用 rebase 模式，可以使用 <code>--rebase</code> 选项。</p>
<h4 id="git-fetch-和-git-pull-的区别">git fetch 和 git pull 的区别</h4>
<p>（1）<code>git fetch</code>：相当于是从远程获取最新版本到本地，不会自动合并。</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git fetch origin master
</span></span><span class="line"><span class="cl">git log -p master..origin/master
</span></span><span class="line"><span class="cl">git merge origin/master
</span></span></code></pre></div><p>以上命令的含义：</p>
<ol>
<li>首先从远程的 <code>origin</code> 的 <code>master</code> 主分支下载最新的版本到 <code>origin/master</code> 分支上</li>
<li>然后比较本地的 <code>master</code> 分支和 <code>origin/master</code> 分支的差别</li>
<li>最后进行合并</li>
</ol>
<p>上述过程其实可以用以下更清晰的方式来进行：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git fetch origin master:tmp
</span></span><span class="line"><span class="cl">git diff tmp
</span></span><span class="line"><span class="cl">git merge tmp
</span></span></code></pre></div><p>（2）<code>git pull</code>：相当于是从远程获取最新版本并 merge 到本地。</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git pull origin master
</span></span></code></pre></div><p>上述命令其实相当于 <code>git fetch</code> 和 <code>git merge</code>。在实际使用中，<code>git fetch</code> 更安全一些，因为在 merge 前，可以查看更新情况，然后再决定是否合并。</p>
<h3 id="8-查看分支">8. 查看分支</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git branch
</span></span></code></pre></div><h3 id="9-创建分支">9. 创建分支</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git branch &lt;name&gt;
</span></span></code></pre></div><h3 id="10-创建并切换到分支">10. 创建并切换到分支</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git checkout -b &lt;name&gt;
</span></span></code></pre></div><p>备注：<code>git checkout</code> 命令加上 <code>-b</code> 参数表示创建并切换，相当于以下两条命令：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git branch &lt;name&gt;
</span></span><span class="line"><span class="cl">git checkout &lt;name&gt;
</span></span></code></pre></div><h3 id="11-切换分支">11. 切换分支</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git checkout &lt;name&gt;
</span></span></code></pre></div><p>切换分支后，在 git bash 中显示为绿色。</p>
<h3 id="12-删除分支">12. 删除分支</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git branch -d &lt;name&gt;
</span></span></code></pre></div><p>如果分支没有合并，删除分支就表示会丢失修改，此时 git 无法使用 <code>-d</code> 删除，可使用 <code>-D</code> 强行删除：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git branch -D &lt;name&gt;
</span></span></code></pre></div><h3 id="13-合并分支">13. 合并分支</h3>
<p>git 合并默认使用 Fast forward 模式，一旦删除分支，会丢掉分支信息，也就看不出来曾经做过合并：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git merge &lt;name&gt;
</span></span></code></pre></div><p>基于当前分支，合并另外一个分支，前提需要保证分支之间不冲突。</p>
<p>如果强制禁用 Fast forward 模式，即普通模式，Git 就会在 merge 时生成一个新的 commit：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git merge --no-ff -m <span class="s2">&#34;there is a comment&#34;</span> &lt;name&gt;
</span></span></code></pre></div><p>因为本次合并要创建一个新的 commit，所以加上 <code>-m</code> 参数，把 commit 描述写进去。工作中，肯定需要不管有没有分支被删除，都要从分支历史上查看所有的历史分支信息，所以要使用普通模式合并。</p>
<h3 id="14-创建-tag">14. 创建 tag</h3>
<p>（1）默认在 HEAD 版本：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git tag &lt;tagname&gt;
</span></span></code></pre></div><p>（2）对指定的 commit 版本创建 tag，需要先找到历史 commit 的 id：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git log --pretty<span class="o">=</span>oneline --abbrev-commit
</span></span><span class="line"><span class="cl">git tag &lt;tagname&gt; &lt;commitid&gt;
</span></span></code></pre></div><p>（3）创建带有说明的 tag，用 <code>-a</code> 指定标签名，<code>-m</code> 指定说明文字：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git tag -a &lt;tagname&gt; -m <span class="s2">&#34;there is a tag description&#34;</span> <span class="o">[</span>&lt;commitid&gt;<span class="o">]</span>
</span></span></code></pre></div><p>（4）通过 <code>-s</code> 用私钥签名一个标签，签名采用 PGP 签名：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git tag -s &lt;tagname&gt; -m <span class="s2">&#34;there is a tag description&#34;</span> <span class="o">[</span>&lt;commitid&gt;<span class="o">]</span>
</span></span></code></pre></div><p>必须首先安装 gpg（GnuPG），如果没有找到 gpg，或者没有 gpg 密钥对，就会报错，参考 GnuPG 帮助文档配置 Key。</p>
<h3 id="15-查看-tag">15. 查看 tag</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git tag    <span class="c1"># 显示的 tag 不是按时间顺序排列，而是按字母顺序排列</span>
</span></span></code></pre></div><p>如果想查看 tag 和 commit 的对应关系：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git log --pretty<span class="o">=</span>oneline --abbrev-commit
</span></span></code></pre></div><p>如果想查看 tag 的详细情况：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git show &lt;tagname&gt;
</span></span></code></pre></div><h3 id="16-删除-tag">16. 删除 tag</h3>
<p>创建的标签都只存储在本地，不会自动推送到远程。所以，打错的标签可以在本地安全删除：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git tag -d &lt;tagname&gt;
</span></span></code></pre></div><p>如果标签已经推送到远程，要删除远程标签就麻烦一点：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git tag -d &lt;tagname&gt;
</span></span><span class="line"><span class="cl">git push origin :refs/tags/&lt;tagname&gt;
</span></span></code></pre></div><h3 id="17-推送标签至远程">17. 推送标签至远程</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git push origin &lt;tagname&gt;
</span></span></code></pre></div><p>或者，一次性推送全部尚未推送到远程的本地标签：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git push origin --tags
</span></span></code></pre></div><h3 id="18-现场的保存与恢复">18. 现场的保存与恢复</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git stash        <span class="c1"># 将目前的工作现场保存</span>
</span></span><span class="line"><span class="cl">git stash list   <span class="c1"># 查看所有保存的工作现场</span>
</span></span></code></pre></div><p>工作现场还在，Git 把 stash 内容存在某个地方了，但是需要恢复一下，有两个办法：</p>
<ol>
<li>用 <code>git stash apply stash@{0}</code> 恢复，但是恢复后，stash 内容并不删除，你需要用 <code>git stash drop stash@{0}</code> 来删除</li>
<li>用 <code>git stash pop</code>，恢复的同时把 stash 内容也删了，这种方式省时省力</li>
</ol>
<p>注意点：</p>
<ol>
<li>如果在分支下新建文件，而尚未执行 <code>add</code> 操作，stash 无法将新文件纳入保存的现场，因为 stash 只对被修改的被追踪的文件和暂存的变更有效，对于新文件必须先执行 <code>add</code></li>
<li>如果修改分支下的已被追踪的文件，不管有没有对修改的文件进行 <code>add</code> 操作，如果执行 stash，所有修改会被纳入保存的现场，而文件会恢复成修改前的状态。恢复现场后，文件又呈现被修改后的状态。特别的是，如果修改的文件在 stash 前已经被 <code>add</code> 了，恢复现场后，暂存区的内容就会清空，相当于这个文件从未被 <code>add</code> 一样</li>
</ol>
<h3 id="19-设置-git-ui-颜色">19. 设置 Git UI 颜色</h3>
<p>让 Git 显示颜色，会让命令输出看起来更醒目：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git config --global color.ui <span class="nb">true</span>
</span></span></code></pre></div><h3 id="20-忽略特殊文件">20. 忽略特殊文件</h3>
<p>（1）在 Git 工作区的根目录下创建一个特殊的 <code>.gitignore</code> 文件，然后把要忽略的文件名填进去，Git 就会自动忽略这些文件。不需要从头写 <code>.gitignore</code> 文件，GitHub 已经为我们准备了各种配置文件，只需要组合一下就可以使用了。所有配置文件可以直接在线浏览：<a href="https://github.com/github/gitignore">https://github.com/github/gitignore</a></p>
<p>忽略文件的原则是：</p>
<ul>
<li>忽略操作系统自动生成的文件，比如缩略图等</li>
<li>忽略编译生成的中间文件、可执行文件等，也就是如果一个文件是通过另一个文件自动生成的，那自动生成的文件就没必要放进版本库，比如 Java 编译产生的 <code>.class</code> 文件</li>
<li>忽略你自己的带有敏感信息的配置文件，比如存放口令的配置文件</li>
</ul>
<p>比如一个完整的 <code>.gitignore</code> 文件，内容如下：</p>
<pre tabindex="0"><code class="language-gitignore" data-lang="gitignore"># Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Python
*.py[cod]
*.so
*.egg
*.egg-info
dist
build
</code></pre><p>（2）把 <code>.gitignore</code> 也提交到 Git：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git add .gitignore
</span></span><span class="line"><span class="cl">git commit -m <span class="s2">&#34;there is a description&#34;</span>
</span></span></code></pre></div><p>就完成了！当然检验 <code>.gitignore</code> 的标准是 <code>git status</code> 命令是不是显示 <code>working tree clean</code>。</p>
<p>使用 Windows 的注意：如果在资源管理器里新建一个 <code>.gitignore</code> 文件，系统会非常弱智地提示必须输入文件名，但是在文本编辑器里「保存」或者「另存为」就可以把文件保存为 <code>.gitignore</code> 了。</p>
<p>（3）如果确实想要添加已经被 <code>.gitignore</code> 忽略的文件，可以用 <code>-f</code> 强制添加到 Git：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git add -f test.class
</span></span></code></pre></div><p>（4）怀疑 <code>.gitignore</code> 写的有问题，需要查找哪个规则写错了，可以用 <code>git check-ignore</code> 命令检查：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git check-ignore -v App.class
</span></span><span class="line"><span class="cl"><span class="c1"># .gitignore:3:*.class    App.class</span>
</span></span></code></pre></div><p>表示 <code>.gitignore</code> 的第 3 行规则忽略了 <code>App.class</code> 这个文件，于是我们就可以知道应该修订哪个规则。</p>
<h3 id="21-为命令配置别名">21. 为命令配置别名</h3>
<p>（1）命令可以简写，用 <code>git st</code> 表示 <code>git status</code>，再比如用 <code>co</code> 表示 <code>checkout</code>、<code>ci</code> 表示 <code>commit</code>、<code>br</code> 表示 <code>branch</code>：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git config --global alias.co checkout
</span></span><span class="line"><span class="cl">git config --global alias.ci commit
</span></span><span class="line"><span class="cl">git config --global alias.br branch
</span></span></code></pre></div><p>以后提交就可以简写成：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git ci -m <span class="s2">&#34;there is a description&#34;</span>
</span></span></code></pre></div><p><code>--global</code> 参数是全局参数，也就是这些命令在这台电脑的所有 Git 仓库下都有用。</p>
<p>（2）命令 <code>git reset HEAD &lt;filename&gt;</code> 可以撤销暂存区的修改（unstage），重新放回工作区。既然是一个 unstage 操作，就可以配置一个 unstage 别名：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git config --global alias.unstage <span class="s1">&#39;reset HEAD&#39;</span>
</span></span><span class="line"><span class="cl">git unstage test.py    <span class="c1"># 等价于 git reset HEAD test.py</span>
</span></span></code></pre></div><p>（3）配置一个 <code>git last</code>，让其显示最后一次提交信息：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git config --global alias.last <span class="s1">&#39;log -1&#39;</span>
</span></span></code></pre></div><p>这样，用 <code>git last</code> 就能显示最近一次的提交：</p>
<pre tabindex="0"><code>Author: xxx &lt;xxx@foxmail.com&gt;
Date:   Mon Apr 23 13:52:44 2018 +0800

    add git ignore list
</code></pre><p>（4）还可以把 <code>lg</code> 配置成：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git config --global alias.lg <span class="s2">&#34;log --color --graph --pretty=format:&#39;%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)&lt;%an&gt;%Creset&#39; --abbrev-commit&#34;</span>
</span></span></code></pre></div><p>来看看 <code>git lg</code> 的效果。</p>
<h3 id="22-修改配置文件">22. 修改配置文件</h3>
<p>配置 Git 的时候，加上 <code>--global</code> 是针对当前用户起作用的，如果不加，那只针对当前的仓库起作用。</p>
<p>每个仓库的 Git 配置文件都放在 <code>.git/config</code> 文件中：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">cat .git/config
</span></span></code></pre></div><div class="highlight"><pre tabindex="0" class="chroma"><code class="language-ini" data-lang="ini"><span class="line"><span class="cl"><span class="k">[core]</span>
</span></span><span class="line"><span class="cl">    <span class="na">repositoryformatversion</span> <span class="o">=</span> <span class="s">0
</span></span></span><span class="line"><span class="cl"><span class="s">    filemode = false
</span></span></span><span class="line"><span class="cl"><span class="s">    bare = false
</span></span></span><span class="line"><span class="cl"><span class="s">    logallrefupdates = true
</span></span></span><span class="line"><span class="cl"><span class="s">    symlinks = false
</span></span></span><span class="line"><span class="cl"><span class="s">    ignorecase = true</span>
</span></span><span class="line"><span class="cl"><span class="k">[branch &#34;master&#34;]</span>
</span></span><span class="line"><span class="cl"><span class="k">[branch &#34;dev&#34;]</span>
</span></span><span class="line"><span class="cl"><span class="k">[remote &#34;github&#34;]</span>
</span></span><span class="line"><span class="cl">    <span class="na">url</span> <span class="o">=</span> <span class="s">git@github.com:xxx/LearnGit.git
</span></span></span><span class="line"><span class="cl"><span class="s">    fetch = +refs/heads/*:refs/remotes/github/*</span>
</span></span><span class="line"><span class="cl"><span class="k">[remote &#34;gitee&#34;]</span>
</span></span><span class="line"><span class="cl">    <span class="na">url</span> <span class="o">=</span> <span class="s">git@gitee.com:xxx/LearnGit.git
</span></span></span><span class="line"><span class="cl"><span class="s">    fetch = +refs/heads/*:refs/remotes/gitee/*</span>
</span></span></code></pre></div><p>而当前用户的 Git 配置文件放在用户主目录下的一个隐藏文件 <code>.gitconfig</code> 中：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-ini" data-lang="ini"><span class="line"><span class="cl"><span class="k">[user]</span>
</span></span><span class="line"><span class="cl">    <span class="na">name</span> <span class="o">=</span> <span class="s">xxx
</span></span></span><span class="line"><span class="cl"><span class="s">    email = xxx@foxmail.com</span>
</span></span><span class="line"><span class="cl"><span class="k">[gpg]</span>
</span></span><span class="line"><span class="cl">    <span class="na">program</span> <span class="o">=</span> <span class="s">C:\\Program Files (x86)\\gnupg\\bin\\gpg.exe</span>
</span></span><span class="line"><span class="cl"><span class="k">[color]</span>
</span></span><span class="line"><span class="cl">    <span class="na">ui</span> <span class="o">=</span> <span class="s">true</span>
</span></span><span class="line"><span class="cl"><span class="k">[alias]</span>
</span></span><span class="line"><span class="cl">    <span class="na">co</span> <span class="o">=</span> <span class="s">checkout
</span></span></span><span class="line"><span class="cl"><span class="s">    ci = commit
</span></span></span><span class="line"><span class="cl"><span class="s">    br = branch
</span></span></span><span class="line"><span class="cl"><span class="s">    last = log -1
</span></span></span><span class="line"><span class="cl"><span class="s">    lg = log --color --graph --pretty=format:&#39;%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)&lt;%an&gt;%Creset&#39; --abbrev-commit</span>
</span></span></code></pre></div><p>别名就在 <code>[alias]</code> 后面，要删除别名，直接把对应的行删掉即可。配置别名也可以直接修改这个文件，如果改错了，可以删掉文件重新通过命令配置。</p>
<h2 id="三多人协作的工作模式">三、多人协作的工作模式</h2>
<p>通常如下：</p>
<ol>
<li>首先将远程仓库克隆为本地仓库：</li>
</ol>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git clone git@github.com:xxx/LearnGit.git
</span></span></code></pre></div><ol start="2">
<li>在本地创建和远程分支对应的分支：</li>
</ol>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git checkout -b &lt;本地分支名&gt; origin/&lt;远程分支名&gt;
</span></span></code></pre></div><p>本地和远程分支的名称最好一致。</p>
<ol start="3">
<li>在本地分支完成任务后，可以试图用 <code>git push &lt;远程主机名&gt; &lt;本地分支名&gt;</code> 推送自己的修改</li>
<li>如果推送失败，则表明远程分支比本地更新，需要先用 <code>git pull</code> 试图合并</li>
<li>如果 pull 失败并提示「no tracking information」，则说明本地分支和远程分支的链接关系没有创建，用命令 <code>git branch --set-upstream-to=&lt;远程主机名&gt;/&lt;远程分支名&gt; &lt;本地分支名&gt;</code> 创建链接</li>
<li>如果合并有冲突，则解决冲突，并在本地提交（<code>add</code> =&gt; <code>commit</code>）</li>
<li>没有冲突或者解决掉冲突后，再用 <code>git push &lt;远程主机名&gt; &lt;本地分支名&gt;</code> 推送就能成功</li>
</ol>
]]></content:encoded>
    </item>
    
    <item>
      <title>Shell 脚本中各种符号意义详解</title>
      <link>https://devcharl.github.io/2023/02/26/shell-learning-note/</link>
      <pubDate>Sun, 26 Feb 2023 16:03:16 +0800</pubDate>
      
      <guid>https://devcharl.github.io/2023/02/26/shell-learning-note/</guid>
      <description>概述 Shell 中有两类字符：普通字符、元字符。 1. 普通字符 在 Shell 中除了本身的字面意思外没有其他特殊意义，即普通纯文本。 2. 元字符 是 Shell 的保留字符，在 Shell 中有着</description>
      <content:encoded><![CDATA[<h2 id="概述">概述</h2>
<p>Shell 中有两类字符：普通字符、元字符。</p>
<h3 id="1-普通字符">1. 普通字符</h3>
<p>在 Shell 中除了本身的字面意思外没有其他特殊意义，即普通纯文本。</p>
<h3 id="2-元字符">2. 元字符</h3>
<p>是 Shell 的保留字符，在 Shell 中有着特殊的含义。</p>
<p><img src="./images/shell-symbol-table.png" alt="Shell 符号表" title="Shell Symbol Table">
<img src="./images/shell-meta-table.png" alt="Shell 元字符表" title="Shell Meta Table"></p>
<h3 id="3-shell-中--的用法">3. Shell 中 <code>$</code> 的用法</h3>
<table>
<thead>
<tr>
<th style="text-align:center">名称</th>
<th style="text-align:left">作用</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center"><code>$#</code></td>
<td style="text-align:left">传给脚本的参数个数</td>
</tr>
<tr>
<td style="text-align:center"><code>$0</code></td>
<td style="text-align:left">脚本本身的名字</td>
</tr>
<tr>
<td style="text-align:center"><code>$1</code></td>
<td style="text-align:left">传递给该shell脚本的第一个参数</td>
</tr>
<tr>
<td style="text-align:center"><code>$2</code></td>
<td style="text-align:left">传递给该shell脚本的第二个参数</td>
</tr>
<tr>
<td style="text-align:center"><code>$@</code></td>
<td style="text-align:left">以一个单字符串显示所有向脚本传递的参数，与位置变量不同，参数可超过9个</td>
</tr>
<tr>
<td style="text-align:center"><code>$$</code></td>
<td style="text-align:left">脚本运行的当前进程ID号</td>
</tr>
<tr>
<td style="text-align:center"><code>$?</code></td>
<td style="text-align:left">显示最后命令的退出状态，0表示没有错误，其他表示有错误</td>
</tr>
</tbody>
</table>
<h3 id="4-实例">4. 实例</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="cp">#!/bin/sh
</span></span></span><span class="line"><span class="cl"><span class="cp"></span><span class="nb">echo</span> <span class="s2">&#34;参数个数:</span><span class="nv">$#</span><span class="s2">&#34;</span>
</span></span><span class="line"><span class="cl"><span class="nb">echo</span> <span class="s2">&#34;脚本名字:</span><span class="nv">$0</span><span class="s2">&#34;</span>
</span></span><span class="line"><span class="cl"><span class="nb">echo</span> <span class="s2">&#34;参数1:</span><span class="nv">$1</span><span class="s2">&#34;</span>
</span></span><span class="line"><span class="cl"><span class="nb">echo</span> <span class="s2">&#34;参数2:</span><span class="nv">$2</span><span class="s2">&#34;</span>
</span></span><span class="line"><span class="cl"><span class="nb">echo</span> <span class="s2">&#34;所有参数列表:</span><span class="nv">$@</span><span class="s2">&#34;</span>
</span></span><span class="line"><span class="cl"><span class="nb">echo</span> <span class="s2">&#34;pid:</span><span class="nv">$$</span><span class="s2">&#34;</span>
</span></span><span class="line"><span class="cl"><span class="k">if</span> <span class="o">[</span> <span class="nv">$1</span> <span class="o">=</span> <span class="m">100</span> <span class="o">]</span>
</span></span><span class="line"><span class="cl"><span class="k">then</span>
</span></span><span class="line"><span class="cl"> <span class="nb">echo</span> <span class="s2">&#34;命令退出状态：</span><span class="nv">$?</span><span class="s2">&#34;</span> 
</span></span><span class="line"><span class="cl"> <span class="nb">exit</span> <span class="m">0</span> <span class="c1">#参数正确，退出状态为0</span>
</span></span><span class="line"><span class="cl"><span class="k">else</span>
</span></span><span class="line"><span class="cl"> <span class="nb">echo</span> <span class="s2">&#34;命令退出状态：</span><span class="nv">$?</span><span class="s2">&#34;</span>
</span></span><span class="line"><span class="cl"> <span class="nb">exit</span> <span class="m">1</span> <span class="c1">#参数错误，退出状态1</span>
</span></span><span class="line"><span class="cl"><span class="k">fi</span>
</span></span></code></pre></div><div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="cp">#!/bin/bash
</span></span></span><span class="line"><span class="cl"><span class="cp"></span><span class="nv">number</span><span class="o">=</span><span class="m">65</span>             <span class="c1">#定义一个退出值</span>
</span></span><span class="line"><span class="cl"><span class="nv">index</span><span class="o">=</span><span class="m">1</span>               <span class="c1">#定义一个计数器</span>
</span></span><span class="line"><span class="cl"><span class="k">if</span> <span class="o">[</span> -z <span class="s2">&#34;</span><span class="nv">$1</span><span class="s2">&#34;</span> <span class="o">]</span><span class="p">;</span><span class="k">then</span>   <span class="c1">#对用户输入的参数做判断，如果未输入参数则返回脚本的用法并退出，退出值65</span>
</span></span><span class="line"><span class="cl">   <span class="nb">echo</span> <span class="s2">&#34;Usage:</span><span class="nv">$0</span><span class="s2"> + 参数&#34;</span>
</span></span><span class="line"><span class="cl">   <span class="nb">exit</span> <span class="nv">$number</span>
</span></span><span class="line"><span class="cl"><span class="k">fi</span>
</span></span><span class="line"><span class="cl"><span class="nb">echo</span> <span class="s2">&#34;listing args with \$*:&#34;</span>    <span class="c1">#在屏幕输入，在$*中遍历参数</span>
</span></span><span class="line"><span class="cl"><span class="k">for</span> arg in <span class="nv">$*</span>                                          
</span></span><span class="line"><span class="cl"><span class="k">do</span>
</span></span><span class="line"><span class="cl">   <span class="nb">echo</span> <span class="s2">&#34;arg: </span><span class="nv">$index</span><span class="s2"> = </span><span class="nv">$arg</span><span class="s2">&#34;</span>                 
</span></span><span class="line"><span class="cl">   <span class="nb">let</span> <span class="nv">index</span><span class="o">+=</span><span class="m">1</span>
</span></span><span class="line"><span class="cl"><span class="k">done</span>
</span></span><span class="line"><span class="cl"><span class="nb">echo</span>
</span></span><span class="line"><span class="cl"><span class="nv">index</span><span class="o">=</span><span class="m">1</span>                         <span class="c1">#将计数器重新设置为1</span>
</span></span><span class="line"><span class="cl"><span class="nb">echo</span> <span class="s2">&#34;listing args with \&#34;\$@\&#34;:&#34;</span>    <span class="c1">#在&#34;$@&#34;中遍历参数</span>
</span></span><span class="line"><span class="cl"><span class="k">for</span> arg in <span class="s2">&#34;</span><span class="nv">$@</span><span class="s2">&#34;</span>
</span></span><span class="line"><span class="cl"><span class="k">do</span>
</span></span><span class="line"><span class="cl">   <span class="nb">echo</span> <span class="s2">&#34;arg: </span><span class="nv">$index</span><span class="s2"> = </span><span class="nv">$arg</span><span class="s2">&#34;</span>
</span></span><span class="line"><span class="cl">   <span class="nb">let</span> <span class="nv">index</span><span class="o">+=</span><span class="m">1</span>
</span></span><span class="line"><span class="cl"><span class="k">done</span>
</span></span></code></pre></div><h3 id="5-错误返回值">5. 错误返回值</h3>
<p>命令或脚本执行后，可通过 <code>$?</code> 查看上一条命令的退出状态：<code>0</code> 表示成功，非 <code>0</code> 通常对应系统 <strong>errno</strong> 错误码。常见 OS errno 如下（完整列表见 <code>errno</code> / <code>man errno</code>）。</p>
<table>
<thead>
<tr>
<th style="text-align:center">错误码</th>
<th style="text-align:left">说明</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center"><code>1</code></td>
<td style="text-align:left">Operation not permitted</td>
</tr>
<tr>
<td style="text-align:center"><code>2</code></td>
<td style="text-align:left">No such file or directory</td>
</tr>
<tr>
<td style="text-align:center"><code>3</code></td>
<td style="text-align:left">No such process</td>
</tr>
<tr>
<td style="text-align:center"><code>4</code></td>
<td style="text-align:left">Interrupted system call</td>
</tr>
<tr>
<td style="text-align:center"><code>5</code></td>
<td style="text-align:left">Input/output error</td>
</tr>
<tr>
<td style="text-align:center"><code>6</code></td>
<td style="text-align:left">No such device or address</td>
</tr>
<tr>
<td style="text-align:center"><code>7</code></td>
<td style="text-align:left">Argument list too long</td>
</tr>
<tr>
<td style="text-align:center"><code>8</code></td>
<td style="text-align:left">Exec format error</td>
</tr>
<tr>
<td style="text-align:center"><code>9</code></td>
<td style="text-align:left">Bad file descriptor</td>
</tr>
<tr>
<td style="text-align:center"><code>10</code></td>
<td style="text-align:left">No child processes</td>
</tr>
<tr>
<td style="text-align:center"><code>11</code></td>
<td style="text-align:left">Resource temporarily unavailable</td>
</tr>
<tr>
<td style="text-align:center"><code>12</code></td>
<td style="text-align:left">Cannot allocate memory</td>
</tr>
<tr>
<td style="text-align:center"><code>13</code></td>
<td style="text-align:left">Permission denied</td>
</tr>
<tr>
<td style="text-align:center"><code>14</code></td>
<td style="text-align:left">Bad address</td>
</tr>
<tr>
<td style="text-align:center"><code>15</code></td>
<td style="text-align:left">Block device required</td>
</tr>
<tr>
<td style="text-align:center"><code>16</code></td>
<td style="text-align:left">Device or resource busy</td>
</tr>
<tr>
<td style="text-align:center"><code>17</code></td>
<td style="text-align:left">File exists</td>
</tr>
<tr>
<td style="text-align:center"><code>18</code></td>
<td style="text-align:left">Invalid cross-device link</td>
</tr>
<tr>
<td style="text-align:center"><code>19</code></td>
<td style="text-align:left">No such device</td>
</tr>
<tr>
<td style="text-align:center"><code>20</code></td>
<td style="text-align:left">Not a directory</td>
</tr>
<tr>
<td style="text-align:center"><code>21</code></td>
<td style="text-align:left">Is a directory</td>
</tr>
<tr>
<td style="text-align:center"><code>22</code></td>
<td style="text-align:left">Invalid argument</td>
</tr>
<tr>
<td style="text-align:center"><code>23</code></td>
<td style="text-align:left">Too many open files in system</td>
</tr>
<tr>
<td style="text-align:center"><code>24</code></td>
<td style="text-align:left">Too many open files</td>
</tr>
<tr>
<td style="text-align:center"><code>25</code></td>
<td style="text-align:left">Inappropriate ioctl for device</td>
</tr>
<tr>
<td style="text-align:center"><code>26</code></td>
<td style="text-align:left">Text file busy</td>
</tr>
<tr>
<td style="text-align:center"><code>27</code></td>
<td style="text-align:left">File too large</td>
</tr>
<tr>
<td style="text-align:center"><code>28</code></td>
<td style="text-align:left">No space left on device</td>
</tr>
<tr>
<td style="text-align:center"><code>29</code></td>
<td style="text-align:left">Illegal seek</td>
</tr>
<tr>
<td style="text-align:center"><code>30</code></td>
<td style="text-align:left">Read-only file system</td>
</tr>
<tr>
<td style="text-align:center"><code>31</code></td>
<td style="text-align:left">Too many links</td>
</tr>
<tr>
<td style="text-align:center"><code>32</code></td>
<td style="text-align:left">Broken pipe</td>
</tr>
<tr>
<td style="text-align:center"><code>33</code></td>
<td style="text-align:left">Numerical argument out of domain</td>
</tr>
<tr>
<td style="text-align:center"><code>34</code></td>
<td style="text-align:left">Numerical result out of range</td>
</tr>
<tr>
<td style="text-align:center"><code>35</code></td>
<td style="text-align:left">Resource deadlock avoided</td>
</tr>
<tr>
<td style="text-align:center"><code>36</code></td>
<td style="text-align:left">File name too long</td>
</tr>
<tr>
<td style="text-align:center"><code>37</code></td>
<td style="text-align:left">No locks available</td>
</tr>
<tr>
<td style="text-align:center"><code>38</code></td>
<td style="text-align:left">Function not implemented</td>
</tr>
<tr>
<td style="text-align:center"><code>39</code></td>
<td style="text-align:left">Directory not empty</td>
</tr>
<tr>
<td style="text-align:center"><code>40</code></td>
<td style="text-align:left">Too many levels of symbolic links</td>
</tr>
<tr>
<td style="text-align:center"><code>42</code></td>
<td style="text-align:left">No message of desired type</td>
</tr>
<tr>
<td style="text-align:center"><code>43</code></td>
<td style="text-align:left">Identifier removed</td>
</tr>
<tr>
<td style="text-align:center"><code>44</code></td>
<td style="text-align:left">Channel number out of range</td>
</tr>
<tr>
<td style="text-align:center"><code>45</code></td>
<td style="text-align:left">Level 2 not synchronized</td>
</tr>
<tr>
<td style="text-align:center"><code>46</code></td>
<td style="text-align:left">Level 3 halted</td>
</tr>
<tr>
<td style="text-align:center"><code>47</code></td>
<td style="text-align:left">Level 3 reset</td>
</tr>
<tr>
<td style="text-align:center"><code>48</code></td>
<td style="text-align:left">Link number out of range</td>
</tr>
<tr>
<td style="text-align:center"><code>49</code></td>
<td style="text-align:left">Protocol driver not attached</td>
</tr>
<tr>
<td style="text-align:center"><code>50</code></td>
<td style="text-align:left">No CSI structure available</td>
</tr>
<tr>
<td style="text-align:center"><code>51</code></td>
<td style="text-align:left">Level 2 halted</td>
</tr>
<tr>
<td style="text-align:center"><code>52</code></td>
<td style="text-align:left">Invalid exchange</td>
</tr>
<tr>
<td style="text-align:center"><code>53</code></td>
<td style="text-align:left">Invalid request descriptor</td>
</tr>
<tr>
<td style="text-align:center"><code>54</code></td>
<td style="text-align:left">Exchange full</td>
</tr>
<tr>
<td style="text-align:center"><code>55</code></td>
<td style="text-align:left">No anode</td>
</tr>
<tr>
<td style="text-align:center"><code>56</code></td>
<td style="text-align:left">Invalid request code</td>
</tr>
<tr>
<td style="text-align:center"><code>57</code></td>
<td style="text-align:left">Invalid slot</td>
</tr>
<tr>
<td style="text-align:center"><code>59</code></td>
<td style="text-align:left">Bad font file format</td>
</tr>
<tr>
<td style="text-align:center"><code>60</code></td>
<td style="text-align:left">Device not a stream</td>
</tr>
<tr>
<td style="text-align:center"><code>61</code></td>
<td style="text-align:left">No data available</td>
</tr>
<tr>
<td style="text-align:center"><code>62</code></td>
<td style="text-align:left">Timer expired</td>
</tr>
<tr>
<td style="text-align:center"><code>63</code></td>
<td style="text-align:left">Out of streams resources</td>
</tr>
<tr>
<td style="text-align:center"><code>64</code></td>
<td style="text-align:left">Machine is not on the network</td>
</tr>
<tr>
<td style="text-align:center"><code>65</code></td>
<td style="text-align:left">Package not installed</td>
</tr>
<tr>
<td style="text-align:center"><code>66</code></td>
<td style="text-align:left">Object is remote</td>
</tr>
<tr>
<td style="text-align:center"><code>67</code></td>
<td style="text-align:left">Link has been severed</td>
</tr>
<tr>
<td style="text-align:center"><code>68</code></td>
<td style="text-align:left">Advertise error</td>
</tr>
<tr>
<td style="text-align:center"><code>69</code></td>
<td style="text-align:left">Srmount error</td>
</tr>
<tr>
<td style="text-align:center"><code>70</code></td>
<td style="text-align:left">Communication error on send</td>
</tr>
<tr>
<td style="text-align:center"><code>71</code></td>
<td style="text-align:left">Protocol error</td>
</tr>
<tr>
<td style="text-align:center"><code>72</code></td>
<td style="text-align:left">Multihop attempted</td>
</tr>
<tr>
<td style="text-align:center"><code>73</code></td>
<td style="text-align:left">RFS specific error</td>
</tr>
<tr>
<td style="text-align:center"><code>74</code></td>
<td style="text-align:left">Bad message</td>
</tr>
<tr>
<td style="text-align:center"><code>75</code></td>
<td style="text-align:left">Value too large for defined data type</td>
</tr>
<tr>
<td style="text-align:center"><code>76</code></td>
<td style="text-align:left">Name not unique on network</td>
</tr>
<tr>
<td style="text-align:center"><code>77</code></td>
<td style="text-align:left">File descriptor in bad state</td>
</tr>
<tr>
<td style="text-align:center"><code>78</code></td>
<td style="text-align:left">Remote address changed</td>
</tr>
<tr>
<td style="text-align:center"><code>79</code></td>
<td style="text-align:left">Can not access a needed shared library</td>
</tr>
<tr>
<td style="text-align:center"><code>80</code></td>
<td style="text-align:left">Accessing a corrupted shared library</td>
</tr>
<tr>
<td style="text-align:center"><code>81</code></td>
<td style="text-align:left">.lib section in a.out corrupted</td>
</tr>
<tr>
<td style="text-align:center"><code>82</code></td>
<td style="text-align:left">Attempting to link in too many shared libraries</td>
</tr>
<tr>
<td style="text-align:center"><code>83</code></td>
<td style="text-align:left">Cannot exec a shared library directly</td>
</tr>
<tr>
<td style="text-align:center"><code>84</code></td>
<td style="text-align:left">Invalid or incomplete multibyte or wide character</td>
</tr>
<tr>
<td style="text-align:center"><code>85</code></td>
<td style="text-align:left">Interrupted system call should be restarted</td>
</tr>
<tr>
<td style="text-align:center"><code>86</code></td>
<td style="text-align:left">Streams pipe error</td>
</tr>
<tr>
<td style="text-align:center"><code>87</code></td>
<td style="text-align:left">Too many users</td>
</tr>
<tr>
<td style="text-align:center"><code>88</code></td>
<td style="text-align:left">Socket operation on non-socket</td>
</tr>
<tr>
<td style="text-align:center"><code>89</code></td>
<td style="text-align:left">Destination address required</td>
</tr>
<tr>
<td style="text-align:center"><code>90</code></td>
<td style="text-align:left">Message too long</td>
</tr>
<tr>
<td style="text-align:center"><code>91</code></td>
<td style="text-align:left">Protocol wrong type for socket</td>
</tr>
<tr>
<td style="text-align:center"><code>92</code></td>
<td style="text-align:left">Protocol not available</td>
</tr>
<tr>
<td style="text-align:center"><code>93</code></td>
<td style="text-align:left">Protocol not supported</td>
</tr>
<tr>
<td style="text-align:center"><code>94</code></td>
<td style="text-align:left">Socket type not supported</td>
</tr>
<tr>
<td style="text-align:center"><code>95</code></td>
<td style="text-align:left">Operation not supported</td>
</tr>
<tr>
<td style="text-align:center"><code>96</code></td>
<td style="text-align:left">Protocol family not supported</td>
</tr>
<tr>
<td style="text-align:center"><code>97</code></td>
<td style="text-align:left">Address family not supported by protocol</td>
</tr>
<tr>
<td style="text-align:center"><code>98</code></td>
<td style="text-align:left">Address already in use</td>
</tr>
<tr>
<td style="text-align:center"><code>99</code></td>
<td style="text-align:left">Cannot assign requested address</td>
</tr>
<tr>
<td style="text-align:center"><code>100</code></td>
<td style="text-align:left">Network is down</td>
</tr>
<tr>
<td style="text-align:center"><code>101</code></td>
<td style="text-align:left">Network is unreachable</td>
</tr>
<tr>
<td style="text-align:center"><code>102</code></td>
<td style="text-align:left">Network dropped connection on reset</td>
</tr>
<tr>
<td style="text-align:center"><code>103</code></td>
<td style="text-align:left">Software caused connection abort</td>
</tr>
<tr>
<td style="text-align:center"><code>104</code></td>
<td style="text-align:left">Connection reset by peer</td>
</tr>
<tr>
<td style="text-align:center"><code>105</code></td>
<td style="text-align:left">No buffer space available</td>
</tr>
<tr>
<td style="text-align:center"><code>106</code></td>
<td style="text-align:left">Transport endpoint is already connected</td>
</tr>
<tr>
<td style="text-align:center"><code>107</code></td>
<td style="text-align:left">Transport endpoint is not connected</td>
</tr>
<tr>
<td style="text-align:center"><code>108</code></td>
<td style="text-align:left">Cannot send after transport endpoint shutdown</td>
</tr>
<tr>
<td style="text-align:center"><code>109</code></td>
<td style="text-align:left">Too many references; cannot splice</td>
</tr>
<tr>
<td style="text-align:center"><code>110</code></td>
<td style="text-align:left">Connection timed out</td>
</tr>
<tr>
<td style="text-align:center"><code>111</code></td>
<td style="text-align:left">Connection refused</td>
</tr>
<tr>
<td style="text-align:center"><code>112</code></td>
<td style="text-align:left">Host is down</td>
</tr>
<tr>
<td style="text-align:center"><code>113</code></td>
<td style="text-align:left">No route to host</td>
</tr>
<tr>
<td style="text-align:center"><code>114</code></td>
<td style="text-align:left">Operation already in progress</td>
</tr>
<tr>
<td style="text-align:center"><code>115</code></td>
<td style="text-align:left">Operation now in progress</td>
</tr>
<tr>
<td style="text-align:center"><code>116</code></td>
<td style="text-align:left">Stale NFS file handle</td>
</tr>
<tr>
<td style="text-align:center"><code>117</code></td>
<td style="text-align:left">Structure needs cleaning</td>
</tr>
<tr>
<td style="text-align:center"><code>118</code></td>
<td style="text-align:left">Not a XENIX named type file</td>
</tr>
<tr>
<td style="text-align:center"><code>119</code></td>
<td style="text-align:left">No XENIX semaphores available</td>
</tr>
<tr>
<td style="text-align:center"><code>120</code></td>
<td style="text-align:left">Is a named type file</td>
</tr>
<tr>
<td style="text-align:center"><code>121</code></td>
<td style="text-align:left">Remote I/O error</td>
</tr>
<tr>
<td style="text-align:center"><code>122</code></td>
<td style="text-align:left">Disk quota exceeded</td>
</tr>
<tr>
<td style="text-align:center"><code>123</code></td>
<td style="text-align:left">No medium found</td>
</tr>
<tr>
<td style="text-align:center"><code>124</code></td>
<td style="text-align:left">Wrong medium type</td>
</tr>
<tr>
<td style="text-align:center"><code>125</code></td>
<td style="text-align:left">Operation canceled</td>
</tr>
<tr>
<td style="text-align:center"><code>126</code></td>
<td style="text-align:left">Required key not available</td>
</tr>
<tr>
<td style="text-align:center"><code>127</code></td>
<td style="text-align:left">Key has expired</td>
</tr>
<tr>
<td style="text-align:center"><code>128</code></td>
<td style="text-align:left">Key has been revoked</td>
</tr>
<tr>
<td style="text-align:center"><code>129</code></td>
<td style="text-align:left">Key was rejected by service</td>
</tr>
<tr>
<td style="text-align:center"><code>130</code></td>
<td style="text-align:left">Owner died</td>
</tr>
<tr>
<td style="text-align:center"><code>131</code></td>
<td style="text-align:left">State not recoverable</td>
</tr>
</tbody>
</table>
<p>以下为文中示例脚本用到的 <strong>MySQL 错误码</strong>（<code>exit</code> 返回值 <code>65</code> 等为自定义退出码，与 errno 不同）。</p>
<table>
<thead>
<tr>
<th style="text-align:center">错误码</th>
<th style="text-align:left">说明</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center"><code>132</code></td>
<td style="text-align:left">Old database file</td>
</tr>
<tr>
<td style="text-align:center"><code>133</code></td>
<td style="text-align:left">No record read before update</td>
</tr>
<tr>
<td style="text-align:center"><code>134</code></td>
<td style="text-align:left">Record was already deleted (or record file crashed)</td>
</tr>
<tr>
<td style="text-align:center"><code>135</code></td>
<td style="text-align:left">No more room in record file</td>
</tr>
<tr>
<td style="text-align:center"><code>136</code></td>
<td style="text-align:left">No more room in index file</td>
</tr>
<tr>
<td style="text-align:center"><code>137</code></td>
<td style="text-align:left">No more records (read after end of file)</td>
</tr>
<tr>
<td style="text-align:center"><code>138</code></td>
<td style="text-align:left">Unsupported extension used for table</td>
</tr>
<tr>
<td style="text-align:center"><code>139</code></td>
<td style="text-align:left">Too big row</td>
</tr>
<tr>
<td style="text-align:center"><code>140</code></td>
<td style="text-align:left">Wrong create options</td>
</tr>
<tr>
<td style="text-align:center"><code>141</code></td>
<td style="text-align:left">Duplicate unique key or constraint on write or update</td>
</tr>
<tr>
<td style="text-align:center"><code>142</code></td>
<td style="text-align:left">Unknown character set used</td>
</tr>
<tr>
<td style="text-align:center"><code>143</code></td>
<td style="text-align:left">Conflicting table definitions in sub-tables of MERGE table</td>
</tr>
<tr>
<td style="text-align:center"><code>144</code></td>
<td style="text-align:left">Table is crashed and last repair failed</td>
</tr>
<tr>
<td style="text-align:center"><code>145</code></td>
<td style="text-align:left">Table was marked as crashed and should be repaired</td>
</tr>
<tr>
<td style="text-align:center"><code>146</code></td>
<td style="text-align:left">Lock timed out; Retry transaction</td>
</tr>
<tr>
<td style="text-align:center"><code>147</code></td>
<td style="text-align:left">Lock table is full;  Restart program with a larger locktable</td>
</tr>
<tr>
<td style="text-align:center"><code>148</code></td>
<td style="text-align:left">Updates are not allowed under a read only transactions</td>
</tr>
<tr>
<td style="text-align:center"><code>149</code></td>
<td style="text-align:left">Lock deadlock; Retry transaction</td>
</tr>
<tr>
<td style="text-align:center"><code>150</code></td>
<td style="text-align:left">Foreign key constraint is incorrectly formed</td>
</tr>
<tr>
<td style="text-align:center"><code>151</code></td>
<td style="text-align:left">Cannot add a child row</td>
</tr>
<tr>
<td style="text-align:center"><code>152</code></td>
<td style="text-align:left">Cannot delete a parent row</td>
</tr>
</tbody>
</table>
]]></content:encoded>
    </item>
    
    <item>
      <title>HTML / PHP 开发踩坑笔记</title>
      <link>https://devcharl.github.io/2022/10/15/web-notes/</link>
      <pubDate>Sat, 15 Oct 2022 16:03:16 +0800</pubDate>
      
      <guid>https://devcharl.github.io/2022/10/15/web-notes/</guid>
      <description>HTML disabled 属性、表格样式、select 选项禁用，以及 PHP 常见问题速查。</description>
      <content:encoded><![CDATA[<p>记录 HTML / JavaScript DOM 操作和 PHP 混排页面时的踩坑笔记。</p>
<h2 id="html--javascript">HTML / JavaScript</h2>
<h3 id="disabled-属性"><code>disabled</code> 属性</h3>
<p><code>disabled</code> 是布尔属性，对 <code>input</code>、<code>button</code>、<code>select</code>、<code>option</code> 等元素有效。注意属性名是 <code>disabled</code>，不是 <code>disable</code>。</p>
<p><strong>标准做法：</strong></p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-javascript" data-lang="javascript"><span class="line"><span class="cl"><span class="kr">const</span> <span class="nx">el</span> <span class="o">=</span> <span class="nb">document</span><span class="p">.</span><span class="nx">getElementById</span><span class="p">(</span><span class="s2">&#34;id&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// 禁用
</span></span></span><span class="line"><span class="cl"><span class="c1"></span><span class="nx">el</span><span class="p">.</span><span class="nx">disabled</span> <span class="o">=</span> <span class="kc">true</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// 取消禁用（推荐）
</span></span></span><span class="line"><span class="cl"><span class="c1"></span><span class="nx">el</span><span class="p">.</span><span class="nx">removeAttribute</span><span class="p">(</span><span class="s2">&#34;disabled&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="nx">el</span><span class="p">.</span><span class="nx">disabled</span> <span class="o">=</span> <span class="kc">false</span><span class="p">;</span>
</span></span></code></pre></div><p><strong>HTML 中的陷阱：</strong></p>
<p>只要 <code>disabled</code> 属性存在，元素就会被禁用，<strong>与属性值无关</strong>：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-html" data-lang="html"><span class="line"><span class="cl"><span class="c">&lt;!-- 以下写法全部处于禁用状态 --&gt;</span>
</span></span><span class="line"><span class="cl"><span class="p">&lt;</span><span class="nt">input</span> <span class="na">type</span><span class="o">=</span><span class="s">&#34;button&#34;</span> <span class="na">disabled</span><span class="o">=</span><span class="s">&#34;false&#34;</span> <span class="na">value</span><span class="o">=</span><span class="s">&#34;&#34;</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="p">&lt;</span><span class="nt">input</span> <span class="na">type</span><span class="o">=</span><span class="s">&#34;button&#34;</span> <span class="na">disabled</span><span class="o">=</span><span class="s">&#34;true&#34;</span> <span class="na">value</span><span class="o">=</span><span class="s">&#34;&#34;</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="p">&lt;</span><span class="nt">input</span> <span class="na">type</span><span class="o">=</span><span class="s">&#34;button&#34;</span> <span class="na">disabled</span><span class="o">=</span><span class="s">&#34;123&#34;</span> <span class="na">value</span><span class="o">=</span><span class="s">&#34;&#34;</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="p">&lt;</span><span class="nt">input</span> <span class="na">type</span><span class="o">=</span><span class="s">&#34;button&#34;</span> <span class="na">disabled</span><span class="o">=</span><span class="s">&#34; &#34;</span> <span class="na">value</span><span class="o">=</span><span class="s">&#34;&#34;</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="p">&lt;</span><span class="nt">input</span> <span class="na">type</span><span class="o">=</span><span class="s">&#34;button&#34;</span> <span class="na">disabled</span><span class="o">=</span><span class="s">&#34;any word&#34;</span> <span class="na">value</span><span class="o">=</span><span class="s">&#34;&#34;</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c">&lt;!-- 以下写法才是不禁用 --&gt;</span>
</span></span><span class="line"><span class="cl"><span class="p">&lt;</span><span class="nt">input</span> <span class="na">type</span><span class="o">=</span><span class="s">&#34;button&#34;</span> <span class="na">value</span><span class="o">=</span><span class="s">&#34;&#34;</span><span class="p">&gt;</span>
</span></span></code></pre></div><p><strong>JS 赋值的行为：</strong></p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-javascript" data-lang="javascript"><span class="line"><span class="cl"><span class="c1">// 以下赋值均会禁用元素
</span></span></span><span class="line"><span class="cl"><span class="c1"></span><span class="nx">el</span><span class="p">.</span><span class="nx">disabled</span> <span class="o">=</span> <span class="kc">true</span>
</span></span><span class="line"><span class="cl"><span class="nx">el</span><span class="p">.</span><span class="nx">disabled</span> <span class="o">=</span> <span class="p">{}</span>
</span></span><span class="line"><span class="cl"><span class="nx">el</span><span class="p">.</span><span class="nx">disabled</span> <span class="o">=</span> <span class="s2">&#34;11&#34;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// 以下赋值可取消禁用
</span></span></span><span class="line"><span class="cl"><span class="c1"></span><span class="nx">el</span><span class="p">.</span><span class="nx">disabled</span> <span class="o">=</span> <span class="kc">false</span>
</span></span><span class="line"><span class="cl"><span class="nx">el</span><span class="p">.</span><span class="nx">disabled</span> <span class="o">=</span> <span class="s2">&#34;&#34;</span>
</span></span><span class="line"><span class="cl"><span class="nx">el</span><span class="p">.</span><span class="nx">disabled</span> <span class="o">=</span> <span class="kc">null</span>
</span></span><span class="line"><span class="cl"><span class="nx">el</span><span class="p">.</span><span class="nx">disabled</span> <span class="o">=</span> <span class="kc">undefined</span>
</span></span><span class="line"><span class="cl"><span class="nx">el</span><span class="p">.</span><span class="nx">removeAttribute</span><span class="p">(</span><span class="s2">&#34;disabled&#34;</span><span class="p">)</span>
</span></span></code></pre></div><h3 id="span"><code>&lt;span&gt;</code></h3>
<p><code>&lt;span&gt;</code> 是行内容器，本身不带样式，通常配合 CSS 对局部文字做修饰：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-html" data-lang="html"><span class="line"><span class="cl"><span class="p">&lt;</span><span class="nt">p</span><span class="p">&gt;</span>普通文字 <span class="p">&lt;</span><span class="nt">span</span> <span class="na">style</span><span class="o">=</span><span class="s">&#34;color: red; font-weight: bold;&#34;</span><span class="p">&gt;</span>高亮文字<span class="p">&lt;/</span><span class="nt">span</span><span class="p">&gt;</span> 继续普通文字<span class="p">&lt;/</span><span class="nt">p</span><span class="p">&gt;</span>
</span></span></code></pre></div><h3 id="table"><code>&lt;table&gt;</code></h3>
<p>默认情况下，<code>&lt;table&gt;</code> 中各列 <code>td</code> 宽度会随最长内容撑开，长文本容易溢出。</p>
<p>推荐组合样式：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-html" data-lang="html"><span class="line"><span class="cl"><span class="p">&lt;</span><span class="nt">table</span> <span class="na">style</span><span class="o">=</span><span class="s">&#34;table-layout: fixed; width: 100%; overflow-wrap: break-word; word-break: break-all;&#34;</span><span class="p">&gt;</span>
</span></span></code></pre></div><table>
<thead>
<tr>
<th>属性</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>table-layout: fixed</code></td>
<td>列宽由表格宽度和列宽设定决定，不随内容无限撑开</td>
</tr>
<tr>
<td><code>overflow-wrap: break-word</code></td>
<td>优先在单词边界换行；仍溢出时再断行（<code>word-wrap</code> 的旧写法）</td>
</tr>
<tr>
<td><code>word-break: break-all</code></td>
<td>允许在任意字符处断行，适合无空格的长字符串</td>
</tr>
</tbody>
</table>
<h3 id="td"><code>&lt;td&gt;</code></h3>
<p>通过 <code>text-align</code> 和 <code>vertical-align</code> 控制单元格内容对齐：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-html" data-lang="html"><span class="line"><span class="cl"><span class="p">&lt;</span><span class="nt">td</span> <span class="na">style</span><span class="o">=</span><span class="s">&#34;text-align: left; vertical-align: middle;&#34;</span><span class="p">&gt;&lt;/</span><span class="nt">td</span><span class="p">&gt;</span>
</span></span></code></pre></div><table>
<thead>
<tr>
<th>属性</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>text-align</code></td>
<td>水平对齐（<code>left</code> / <code>center</code> / <code>right</code>）</td>
</tr>
<tr>
<td><code>vertical-align</code></td>
<td>垂直对齐（<code>top</code> / <code>middle</code> / <code>bottom</code>）</td>
</tr>
</tbody>
</table>
<h3 id="select"><code>&lt;select&gt;</code></h3>
<p>按选项文本禁用 <code>select</code> 中的特定 <code>option</code>：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-javascript" data-lang="javascript"><span class="line"><span class="cl"><span class="kr">const</span> <span class="nx">select</span> <span class="o">=</span> <span class="nb">document</span><span class="p">.</span><span class="nx">getElementById</span><span class="p">(</span><span class="s2">&#34;WlanAuthMode_select&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="kr">const</span> <span class="nx">disabledValues</span> <span class="o">=</span> <span class="k">new</span> <span class="nx">Set</span><span class="p">([</span>
</span></span><span class="line"><span class="cl">  <span class="s2">&#34;Shared&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">  <span class="s2">&#34;WPA Pre-Shared Key&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">  <span class="s2">&#34;WPA/WPA2 Pre-Shared Key&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl"><span class="p">]);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">for</span> <span class="p">(</span><span class="kr">const</span> <span class="nx">option</span> <span class="k">of</span> <span class="nx">select</span><span class="p">.</span><span class="nx">options</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="k">if</span> <span class="p">(</span><span class="nx">disabledValues</span><span class="p">.</span><span class="nx">has</span><span class="p">(</span><span class="nx">option</span><span class="p">.</span><span class="nx">text</span><span class="p">))</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="nx">option</span><span class="p">.</span><span class="nx">disabled</span> <span class="o">=</span> <span class="kc">true</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">  <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>注意：</p>
<ul>
<li>遍历 <code>options</code> 应使用 <code>for...of</code> 或索引循环，不要用 <code>for...in</code></li>
<li>读取选项文字用 <code>option.text</code> 或 <code>option.textContent</code>，不要用 <code>innerText</code></li>
<li>JS 中设置禁应用 <code>option.disabled = true</code>，不要赋字符串 <code>&quot;disabled&quot;</code></li>
</ul>
<h2 id="php">PHP</h2>
<h3 id="未解决问题">未解决问题</h3>
<p>在 PHP 与 HTML 混排的页面中，若 JavaScript 代码写在 PHP 输出逻辑之外，或 <code>&lt;script&gt;</code> 标签未正确闭合，可能导致后续 HTML 无法渲染，页面表现为「全部空白」。</p>
<p>当时尝试在 PHP 中补一行：</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-php" data-lang="php"><span class="line"><span class="cl"><span class="k">echo</span> <span class="s1">&#39;&lt;script language=&#34;javascript&#34;&gt;&lt;/script&gt;&#39;</span><span class="p">;</span>
</span></span></code></pre></div><p>页面才恢复显示，但根因尚未定位。<code>language</code> 属性在 HTML5 中已废弃，后续应优先检查：</p>
<ol>
<li><code>&lt;script&gt;</code> 标签是否成对闭合</li>
<li>PHP 的 <code>echo</code> / <code>print</code> 是否打断了 HTML 结构</li>
<li>浏览器控制台是否有 JS 语法错误导致解析中断</li>
</ol>
<h3 id="已解决问题">已解决问题</h3>
<table>
<thead>
<tr>
<th>问题</th>
<th>解决方法</th>
<th>备注</th>
</tr>
</thead>
<tbody>
<tr>
<td>自定义 JS 函数报错</td>
<td>将函数定义放在 <code>&lt;script&gt;...&lt;/script&gt;</code> 块内</td>
<td>不要写在 PHP 字符串拼接的半截位置</td>
</tr>
<tr>
<td>避免重复加载文件</td>
<td>使用 <code>require_once</code></td>
<td>防止函数/常量重复定义</td>
</tr>
<tr>
<td>设置框架组成和属性</td>
<td>使用 <code>frameset</code> / <code>frame</code></td>
<td>HTML5 已废弃，新项目勿用</td>
</tr>
<tr>
<td>检查变量是否已设置</td>
<td>使用 <code>isset($var)</code></td>
<td>避免未定义变量告警</td>
</tr>
<tr>
<td>忽略错误输出</td>
<td>在表达式前加 <code>@</code></td>
<td>如 <code>@fopen(...)</code>，仅用于明确可忽略的场景</td>
</tr>
<tr>
<td>获取数组当前键名</td>
<td>使用 <code>key($array)</code></td>
<td>配合 <code>next()</code> / <code>reset()</code> 遍历</td>
</tr>
<tr>
<td>将数组指针移到第一个元素</td>
<td>使用 <code>reset($array)</code></td>
<td>返回第一个元素的值</td>
</tr>
<tr>
<td>将数组指针移到下一个元素</td>
<td>使用 <code>next($array)</code></td>
<td>返回下一个元素的值</td>
</tr>
<tr>
<td>PHP 短标签输出</td>
<td>使用 <code>&lt;?php ?&gt;</code></td>
<td><code>&lt;? ?&gt;</code> 依赖 <code>short_open_tag</code>，很多环境默认关闭</td>
</tr>
</tbody>
</table>
]]></content:encoded>
    </item>
    
    <item>
      <title>D-Bus 学习笔记 01</title>
      <link>https://devcharl.github.io/2022/09/07/dbus-learning-notes-01/</link>
      <pubDate>Wed, 07 Sep 2022 15:47:20 +0800</pubDate>
      
      <guid>https://devcharl.github.io/2022/09/07/dbus-learning-notes-01/</guid>
      <description>基础概念 D-Bus 是一个消息总线系统，其功能已涵盖进程间通信的所有需求，并具备一些特殊的用途 D-Bus 是三层架构的进程间通信系统：接口层 + 总线层 + 包装层 接口</description>
      <content:encoded><![CDATA[<h2 id="基础概念">基础概念</h2>
<ul>
<li>D-Bus 是一个消息总线系统，其功能已涵盖进程间通信的所有需求，并具备一些特殊的用途</li>
<li>D-Bus 是三层架构的进程间通信系统：接口层 + 总线层 + 包装层
<ul>
<li><strong>接口层</strong>：由函数库 <code>libdbus</code> 提供，进程可通过该库使用 D-Bus 的能力</li>
<li><strong>总线层</strong>：由 D-Bus 总线守护进程提供，在 Linux 系统启动时运行，负责进程间的消息路由和传递，其中包括 Linux 内核和 Linux 桌面环境的消息传递</li>
<li><strong>包装层</strong>：一系列基于特定应用程序框架的 Wrapper 库</li>
</ul>
</li>
<li>D-Bus 具备自身的协议，协议基于二进制数据设计，与数据结构和编码方式无关
<ul>
<li><code>libdbus</code> / D-Bus 总线守护进程均不需要太大的系统开销</li>
</ul>
</li>
</ul>
<h3 id="总线类型">总线类型</h3>
<p>总线是 D-Bus 的进程间通信机制，一个系统中通常存在多条总线，这些总线由 D-Bus 总线守护进程管理：</p>
<ul>
<li><strong>系统总线（System Bus）</strong>：只有 Linux 内核、Linux 桌面环境和权限较高的程序才能向该总线写入消息，以此保障系统安全性，防止有恶意进程假冒 Linux 发送消息</li>
<li><strong>会话总线（Session Buses）</strong>：由普通进程创建，可同时存在多条。会话总线属于某个进程私有，它用于进程间传递消息</li>
</ul>
<h3 id="重要概念">重要概念</h3>
<ul>
<li><strong>对象</strong>：对象是封装后的匹配器与回调函数，它以对等（peer-to-peer）协议使每个消息都有一个源地址和一个目的地址。这些地址又称为对象路径，或者称之为总线名称。对象的接口是回调函数，它以类似 C++ 的虚拟函数实现。当一个进程注册到某个总线时，都要创建相应的消息对象</li>
<li><strong>消息</strong>：D-Bus 的消息分为信号（signals）、方法调用（method calls）、方法返回（method returns）和错误（errors）
<ul>
<li><strong>信号</strong>：最基本的消息，注册的进程可简单地发送信号到总线上，其他进程通过总线读取消息</li>
<li><strong>方法调用</strong>：通过总线传递参数，执行另一个进程接口函数的机制，用于某个进程控制另一个进程</li>
<li><strong>方法返回</strong>：注册的进程在收到相关信息后，自动做出反应的机制，由回调函数实现</li>
<li><strong>错误</strong>：信号的一种，是注册进程错误处理机制之一</li>
</ul>
</li>
<li><strong>服务</strong>：服务（Services）是进程注册的抽象。进程注册某个地址后，即可获得对应总线的服务。D-Bus 提供了服务查询接口，进程可通过该接口查询某个服务是否存在，或者在服务结束时自动收到来自系统的消息</li>
</ul>
<h2 id="典型流程">典型流程</h2>
<h3 id="建立服务">建立服务</h3>
<ol>
<li>建立 dbus 连接（<code>dbus_bus_get()</code>）</li>
<li>为这个 dbus 连接（<code>DbusConnection</code>）起名（<code>dbus_bus_request_name()</code>），此名即为后续远程调用的服务名</li>
<li>进入监听循环（<code>dbus_connection_read_write()</code>）</li>
<li>循环中，从总线中取出信息（<code>dbus_connection_pop_message()</code>）</li>
<li>通过比对信息中的方法接口名和方法名（<code>dbus_message_is_method_call()</code>），如果一致，跳转到相应的处理中，从消息中取出远程调用的参数，并建立回传<sup id="fnref:1"><a href="#fn:1" class="footnote-ref" role="doc-noteref">1</a></sup>结果的通路（<code>reply_to_method_call()</code>）</li>
</ol>
<h3 id="发送信号">发送信号</h3>
<ol>
<li>建立 dbus 连接，并为这个连接起名，建立发送信号的通道</li>
<li>在建立通道的函数中，需要填写该信号的接口名及信号名（<code>dbus_message_new_signal()</code>）</li>
<li>将相关的参数压到对应的信号中去（<code>dbus_message_iter_init_append()</code>、<code>dbus_message_iter_append_basic()</code>）</li>
<li>启动发送（<code>dbus_connection_send()</code>、<code>dbus_connection_flush()</code>）</li>
</ol>
<h3 id="进行远程调用">进行远程调用</h3>
<ol>
<li>建立 dbus 连接，并为 dbus 连接命名</li>
<li>申请一个远程调用通道（<code>dbus_message_new_method_call()</code>），注意填写服务器名，及本次调用的接口名 + 方法名，压入本次调用的参数（<code>dbus_message_iter_init_append()</code>、<code>dbus_message_iter_append_basic()</code>）</li>
<li>把真正要传的参数写入消息结构（送完之后一般都会判断是否越界）</li>
<li>启动发送调用并释放发送相关的消息结构（<code>dbus_connection_send_with_reply()</code>），启动函数中带有一个句柄，之后阻塞等待句柄带回总线上回传的信息，当句柄回传信息之后，从信息结构中分离出参数（<code>dbus_message_iter_init()</code>、<code>dbus_message_iter_next()</code>、<code>dbus_message_iter_get_arg_type()</code>、<code>dbus_message_iter_get_basic()</code>）</li>
</ol>
<h3 id="信号接收">信号接收</h3>
<ol>
<li>建立 dbus 连接并为 dbus 连接起名，为要进行的消息循环添加匹配条件（通过信号名和信号接口名来进行匹配控制）（<code>dbus_bus_add_match()</code>）。进入等待循环后，只需要对信号名、信号接口名进行判断就可以分别处理各种信号了</li>
</ol>
<h2 id="常用-api-参考">常用 API 参考</h2>
<table>
<thead>
<tr>
<th>函数</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>dbus_connection_read_write()</code></td>
<td>在连接打开时阻塞，直到可以读或写，然后执行读写操作</td>
</tr>
<tr>
<td><code>dbus_connection_pop_message()</code></td>
<td>从入站消息队列中取出第一条消息并移除；队列为空时返回 <code>NULL</code></td>
</tr>
<tr>
<td><code>dbus_connection_send()</code></td>
<td>将消息加入出站队列，异步写入网络；要强制写出可调用 <code>dbus_connection_flush()</code></td>
</tr>
<tr>
<td><code>dbus_connection_send_with_reply()</code></td>
<td>发送消息并返回 <code>DBusPendingCall</code>，用于接收回复；超时则生成合成错误回复</td>
</tr>
<tr>
<td><code>dbus_message_is_signal()</code></td>
<td>检查消息是否为具有给定 interface 和 member 字段的信号</td>
</tr>
<tr>
<td><code>dbus_message_iter_init()</code></td>
<td>初始化 <code>DBusMessageIter</code> 以读取消息参数</td>
</tr>
<tr>
<td><code>dbus_message_iter_next()</code></td>
<td>移动迭代器到下一个字段；无下一个字段时返回 <code>FALSE</code></td>
</tr>
<tr>
<td><code>dbus_message_iter_get_arg_type()</code></td>
<td>返回迭代器指向的参数类型；迭代器在消息末尾时返回 <code>DBUS_TYPE_INVALID</code></td>
</tr>
<tr>
<td><code>dbus_message_iter_get_basic()</code></td>
<td>从消息迭代器读取基本类型值（整数、字符串等非容器类型）</td>
</tr>
<tr>
<td><code>dbus_message_new_signal()</code></td>
<td>构造表示信号发射的新消息</td>
</tr>
<tr>
<td><code>dbus_message_iter_init_append()</code></td>
<td>初始化 <code>DBusMessageIter</code> 以向消息末尾追加参数</td>
</tr>
<tr>
<td><code>dbus_message_iter_append_basic()</code></td>
<td>向消息追加基本类型值</td>
</tr>
<tr>
<td><code>dbus_message_new_method_call()</code></td>
<td>构造调用远程对象方法的新消息</td>
</tr>
<tr>
<td><code>dbus_bus_get()</code></td>
<td>连接到总线守护进程并注册客户端；若连接已存在则返回已有连接</td>
</tr>
<tr>
<td><code>dbus_bus_request_name()</code></td>
<td>请求总线将给定名称分配给此连接</td>
</tr>
<tr>
<td><code>dbus_bus_add_match()</code></td>
<td>添加匹配规则以匹配经过消息总线的消息</td>
</tr>
<tr>
<td><code>dbus_pending_call_block()</code></td>
<td>阻塞直到 pending call 完成</td>
</tr>
<tr>
<td><code>dbus_pending_call_steal_reply()</code></td>
<td>获取回复消息；每条 pending call 只能调用一次</td>
</tr>
</tbody>
</table>
<h3 id="命名规则">命名规则</h3>
<p>给 <code>DBusConnection</code> 起名字（命名）时，两个相互通信的连接（connection）不能同名。</p>
<p>命名规则：<code>xxx.xxx</code>（例如 <code>zeng.xiaolong</code>）</p>
<div class="footnotes" role="doc-endnotes">
<hr>
<ol>
<li id="fn:1">
<p>回传动作本身等同于一次不需要等待结果的远程调用。&#160;<a href="#fnref:1" class="footnote-backref" role="doc-backlink">&#x21a9;&#xfe0e;</a></p>
</li>
</ol>
</div>
]]></content:encoded>
    </item>
    
    <item>
      <title>VS Code 正则表达式与快捷键</title>
      <link>https://devcharl.github.io/2022/08/24/vscode-learning-note/</link>
      <pubDate>Wed, 24 Aug 2022 16:03:16 +0800</pubDate>
      
      <guid>https://devcharl.github.io/2022/08/24/vscode-learning-note/</guid>
      <description>正则表达式 用途 表达式 删除空行 ^\s*(?=\r?$)\n 常用快捷键 操作 快捷键 打开键盘快捷键设置 Ctrl+K Ctrl+S，或 File → Preferences → Keyboard Shortcuts 当前行快速换行 Ctrl+Enter 快速删除当前行 Shift+Delete 或 Ctrl+Shift+K 查找 Ctrl+F</description>
      <content:encoded><![CDATA[<h2 id="正则表达式">正则表达式</h2>
<table>
<thead>
<tr>
<th>用途</th>
<th>表达式</th>
</tr>
</thead>
<tbody>
<tr>
<td>删除空行</td>
<td><code>^\s*(?=\r?$)\n</code></td>
</tr>
</tbody>
</table>
<h2 id="常用快捷键">常用快捷键</h2>
<table>
<thead>
<tr>
<th>操作</th>
<th>快捷键</th>
</tr>
</thead>
<tbody>
<tr>
<td>打开键盘快捷键设置</td>
<td><code>Ctrl+K Ctrl+S</code>，或 File → Preferences → Keyboard Shortcuts</td>
</tr>
<tr>
<td>当前行快速换行</td>
<td><code>Ctrl+Enter</code></td>
</tr>
<tr>
<td>快速删除当前行</td>
<td><code>Shift+Delete</code> 或 <code>Ctrl+Shift+K</code></td>
</tr>
<tr>
<td>查找</td>
<td><code>Ctrl+F</code></td>
</tr>
<tr>
<td>文件中查找</td>
<td><code>Ctrl+Shift+F</code></td>
</tr>
<tr>
<td>替换</td>
<td><code>Ctrl+H</code></td>
</tr>
<tr>
<td>文件中替换</td>
<td><code>Ctrl+Shift+H</code></td>
</tr>
</tbody>
</table>
]]></content:encoded>
    </item>
    
    <item>
      <title>Iperf3 网络性能测试工具</title>
      <link>https://devcharl.github.io/2020/04/17/iperf3-learning-note/</link>
      <pubDate>Fri, 17 Apr 2020 16:03:16 +0800</pubDate>
      
      <guid>https://devcharl.github.io/2020/04/17/iperf3-learning-note/</guid>
      <description>Iperf3 简介、安装、常用参数与 TCP/UDP 测试实例，以及 Jperf 图形界面工具介绍。</description>
      <content:encoded><![CDATA[<p>网络性能评估主要是监测网络带宽的使用率，将网络带宽利用最大化是保证网络性能的基础。但由于网络设计不合理、网络存在安全漏洞等原因，都会导致网络带宽利用率不高。要找到网络带宽利用率不高的原因，就需要对网络传输进行监控，此时就需要用到一些网络性能评估工具，而 Iperf 就是这样一款网络带宽测试工具。</p>
<h2 id="iperf-简介">Iperf 简介</h2>
<h3 id="什么是-iperf">什么是 iperf？</h3>
<p>Iperf 是美国伊利诺斯大学（University of Illinois）开发的一种开源的网络性能测试工具，可以用来测试网络节点间（也包括回环）TCP 或 UDP 连接的性能，包括带宽、抖动以及丢包率。其中抖动和丢包率适用于 UDP 测试，而带宽测试适用于 TCP 和 UDP。</p>
<p>Iperf 是一款基于 TCP/IP 和 UDP/IP 的网络性能测试工具，可以用来测量网络带宽和网络质量，提供网络延迟抖动、数据包丢失率、最大传输单元等统计信息。网络管理员可以根据这些信息了解并判断网络性能问题，从而定位网络瓶颈，解决网络故障。</p>
<p>Iperf 是一款基于命令行模式的网络性能测试工具，是跨平台的，提供横跨 Windows、Linux、Mac 的全平台支持。iperf 全程使用内存作为发送/接收缓冲区，不受磁盘性能的影响，对于机器配置要求很低。不过由于是命令行工具，iperf 不支持输出测试图形。</p>
<p>Iperf 可以测试 TCP 和 UDP 带宽质量，具有多种参数和 UDP 特性，可以用来测试一些网络设备如路由器、防火墙、交换机等的性能。</p>
<h3 id="iperf-的功能">Iperf 的功能</h3>
<p><strong>（1）TCP 方面</strong></p>
<ul>
<li>测量网络带宽</li>
<li>报告 MSS/MTU 值的大小和观测值</li>
<li>支持 TCP 窗口值通过套接字缓冲</li>
<li>当 P 线程或 Win32 线程可用时，支持多线程。客户端与服务端支持同时多重连接</li>
</ul>
<p><strong>（2）UDP 方面</strong></p>
<ul>
<li>客户端可以创建指定带宽的 UDP 流</li>
<li>测量丢包</li>
<li>测量延迟</li>
<li>支持多播</li>
<li>当 P 线程可用时，支持多线程。客户端与服务端支持同时多重连接（不支持 Windows）</li>
</ul>
<p><strong>（3）其他方面</strong></p>
<ul>
<li>在适当的地方，选项中可以使用 K（kilo-）和 M（mega-）。例如 131072 字节可以用 128K 代替</li>
<li>可以指定运行的总时间，甚至可以设置传输的数据总量</li>
<li>在报告中，为数据选用最合适的单位</li>
<li>服务器支持多重连接，而不是等待一个单线程测试</li>
<li>在指定时间间隔重复显示网络带宽、波动和丢包情况</li>
<li>服务器端可作为后台程序运行</li>
<li>服务器端可作为 Windows 服务运行</li>
<li>使用典型数据流来测试链接层压缩对于可用带宽的影响</li>
</ul>
<h2 id="iperf-的安装">Iperf 的安装</h2>
<h3 id="iperf-的版本">iperf 的版本</h3>
<p>Iperf 有两种版本，Windows 版和 Linux 版本。</p>
<p><strong>（1）Unix/Linux 版</strong></p>
<p>更新比较快，版本最新，目前最新的版本是 iperf3.0。</p>
<p>Linux 版本下载地址：<a href="http://code.google.com/p/iperf/downloads/list">http://code.google.com/p/iperf/downloads/list</a></p>
<p>为了测试的准确性，尽量使用 Linux 环境测试。</p>
<p><strong>（2）Windows 版</strong></p>
<p>Windows 版 iperf 叫 jperf，或者 xjperf，更新慢，目前最新版本为 1.7（打包在 jperf 中）。</p>
<p>Windows 版本下载地址：<a href="http://sourceforge.net/projects/iperf/files/jperf/jperf%202.0.0/">http://sourceforge.net/projects/iperf/files/jperf/jperf%202.0.0/</a></p>
<p>jperf 是在 iperf 基础上开发的图形界面程序，简化了复杂命令行参数的构造，而且还能保存测试结果，同时实时图形化显示结果。</p>
<h3 id="windows-版-iperf-安装">Windows 版 iperf 安装</h3>
<p>对于 Windows 版的 iperf，下载安装包后直接解压，然后将解压出来的 <code>iperf.exe</code> 和 <code>cygwin1.dll</code> 复制到 <code>%systemroot%</code> 目录即可。</p>
<h3 id="linux-版-iperf-安装">Linux 版 iperf 安装</h3>
<p><strong>（1）在线安装</strong></p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># CentOS</span>
</span></span><span class="line"><span class="cl">yum install -y iperf3
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Debian / Ubuntu</span>
</span></span><span class="line"><span class="cl">apt-get install iperf3
</span></span></code></pre></div><p><strong>（2）离线安装</strong></p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">gunzip -c iperf-&lt;version&gt;.tar.gz <span class="p">|</span> tar -xvf -
</span></span><span class="line"><span class="cl"><span class="nb">cd</span> iperf-&lt;version&gt;
</span></span><span class="line"><span class="cl">./configure
</span></span><span class="line"><span class="cl">make
</span></span><span class="line"><span class="cl">make install
</span></span></code></pre></div><h2 id="iperf-的使用">Iperf 的使用</h2>
<h3 id="iperf-的工作模式">Iperf 的工作模式</h3>
<p>Iperf 可以运行在任何 IP 网络上，包括本地以太网、接入因特网、Wi-Fi 网络等。在工作模式上，iperf 运行于服务器、客户端模式下，其服务器端主要用于监听到达的测试请求，而客户端主要用于发起测试连接会话，因此要使用 iperf 至少需要两台服务器，一台运行在服务器模式下，另一台运行在客户端模式下。</p>
<p>在完成 iperf 安装后，执行 <code>iperf3 -h</code> 即可显示 iperf 的详细用法。iperf 的命令行选项共分为三类，分别是客户端与服务器端公用选项、服务器端专用选项和客户端专用选项。</p>
<h3 id="iperf-常用参数测试够用">Iperf 常用参数（测试够用）</h3>
<table>
<thead>
<tr>
<th>参数</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>-s, --server</code></td>
<td>iperf 服务器模式，默认启动的监听端口为 5201，如：<code>iperf -s</code></td>
</tr>
<tr>
<td><code>-c, --client host</code></td>
<td>iperf 客户端模式，host 是 server 端地址，如：<code>iperf -c 222.35.11.23</code></td>
</tr>
<tr>
<td><code>-i, --interval</code></td>
<td>指定每次报告之间的时间间隔，单位为秒，如：<code>iperf3 -c 192.168.12.168 -i 2</code></td>
</tr>
<tr>
<td><code>-p, --port</code></td>
<td>指定服务器端监听的端口或客户端所连接的端口，默认是 5001 端口</td>
</tr>
<tr>
<td><code>-u, --udp</code></td>
<td>表示采用 UDP 协议发送报文，不带该参数表示采用 TCP 协议</td>
</tr>
<tr>
<td><code>-l, --len</code></td>
<td>设置读写缓冲区的长度，单位为 Byte。TCP 方式默认为 8KB，UDP 方式默认为 1470 字节。通常测试 PPS 的时候该值为 16，测试 BPS 时该值为 1400</td>
</tr>
<tr>
<td><code>-b, --bandwidth [K|M|G]</code></td>
<td>指定 UDP 模式使用的带宽，单位 bits/sec，默认值是 1 Mbit/sec</td>
</tr>
<tr>
<td><code>-t, --time</code></td>
<td>指定数据传输的总时间，即在指定的时间内，重复发送指定长度的数据包。默认 10 秒</td>
</tr>
<tr>
<td><code>-A</code></td>
<td>CPU 亲和性，可以将具体的 iperf3 进程绑定对应编号的逻辑 CPU，避免 iperf 进程在不同的 CPU 间调度</td>
</tr>
</tbody>
</table>
<h3 id="通用参数server-端和-client-端共用">通用参数（Server 端和 Client 端共用）</h3>
<table>
<thead>
<tr>
<th>参数</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>-f, --format [k|m|g|K|M|G]</code></td>
<td>指定带宽输出单位，<code>[k|m|g|K|M|G]</code> 分别表示以 Kbits、Mbits、Gbits、KBytes、MBytes、GBytes 显示输出结果，默认 Mbits，如：<code>iperf3 -c 192.168.12.168 -f M</code></td>
</tr>
<tr>
<td><code>-p, --port</code></td>
<td>指定服务器端监听的端口或客户端所连接的端口，默认是 5001 端口</td>
</tr>
<tr>
<td><code>-i, --interval</code></td>
<td>指定每次报告之间的时间间隔，单位为秒，如：<code>iperf3 -c 192.168.12.168 -i 2</code></td>
</tr>
<tr>
<td><code>-F</code></td>
<td>指定文件作为数据流进行带宽测试，如：<code>iperf3 -c 192.168.12.168 -F web-ixdba.tar.gz</code></td>
</tr>
</tbody>
</table>
<h3 id="server-端专用参数">Server 端专用参数</h3>
<table>
<thead>
<tr>
<th>参数</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>-s, --server</code></td>
<td>iperf 服务器模式，默认启动的监听端口为 5201，如：<code>iperf -s</code></td>
</tr>
<tr>
<td><code>-c, --client host</code></td>
<td>如果 iperf 运行在服务器模式，并且用 <code>-c</code> 参数指定一个主机，那么 iperf 将只接受指定主机的连接。此参数不能工作于 UDP 模式</td>
</tr>
<tr>
<td><code>-D</code></td>
<td>Unix 平台下将 Iperf 作为后台守护进程运行。在 Win32 平台下，Iperf 将作为服务运行</td>
</tr>
<tr>
<td><code>-R</code></td>
<td>卸载 Iperf 服务（仅用于 Windows）</td>
</tr>
<tr>
<td><code>-o</code></td>
<td>重定向输出到指定文件（仅用于 Windows）</td>
</tr>
<tr>
<td><code>-P, --parallel</code></td>
<td>服务器关闭之前保持的连接数。默认是 0，这意味着永远接受连接</td>
</tr>
</tbody>
</table>
<h3 id="client-端专用参数">Client 端专用参数</h3>
<table>
<thead>
<tr>
<th>参数</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>-c, --client host</code></td>
<td>iperf 客户端模式，host 是 server 端地址，如：<code>iperf -c 222.35.11.23</code></td>
</tr>
<tr>
<td><code>-u, --udp</code></td>
<td>表示采用 UDP 协议发送报文，不带该参数表示采用 TCP 协议</td>
</tr>
<tr>
<td><code>-b, --bandwidth [K|M|G]</code></td>
<td>指定 UDP 模式使用的带宽，单位 bits/sec，默认值是 1 Mbit/sec</td>
</tr>
<tr>
<td><code>-t, --time</code></td>
<td>指定数据传输的总时间，即在指定的时间内，重复发送指定长度的数据包。默认 10 秒</td>
</tr>
<tr>
<td><code>-l, --len</code></td>
<td>设置读写缓冲区的长度，单位为 Byte。TCP 默认为 8KB，UDP 默认为 1470 字节。通常测试 PPS 的时候该值为 16，测试 BPS 时该值为 1400</td>
</tr>
<tr>
<td><code>-n, --num [K|M|G]</code></td>
<td>指定传输数据包的字节数，如：<code>iperf3 -c 192.168.12.168 -n 100M</code></td>
</tr>
<tr>
<td><code>-P, --parallel</code></td>
<td>指定客户端与服务端之间使用的线程数。默认是 1 个线程。需要客户端与服务器端同时使用此参数</td>
</tr>
<tr>
<td><code>-w, --window</code></td>
<td>指定套接字缓冲区大小，在 TCP 方式下，此设置为 TCP 窗口的大小。在 UDP 方式下，此设置为接受 UDP 数据包的缓冲区大小，用来限制可以接收数据包的最大值</td>
</tr>
<tr>
<td><code>-B, --bind</code></td>
<td>用来绑定一个主机地址或接口，这个参数仅用于具有多个网络接口的主机。在 UDP 模式下，此参数用于绑定和加入一个多播组</td>
</tr>
<tr>
<td><code>-M, --mss</code></td>
<td>设置 TCP 最大信息段的值</td>
</tr>
<tr>
<td><code>-N, --nodelay</code></td>
<td>设置 TCP 无延时</td>
</tr>
<tr>
<td><code>-V</code></td>
<td>绑定一个 IPv6 地址</td>
</tr>
<tr>
<td><code>-d, --dualtest</code></td>
<td>运行双测试模式。将使服务器端反向连接到客户端，使用 <code>-L</code> 参数中指定的端口（或默认使用客户端连接到服务器端的端口）。使用参数 <code>-r</code> 以运行交互模式</td>
</tr>
<tr>
<td><code>-L, --listenport</code></td>
<td>指定服务端反向连接到客户端时使用的端口。默认使用客户端连接至服务端的端口</td>
</tr>
<tr>
<td><code>-r, --tradeoff</code></td>
<td>往复测试模式。当客户端到服务器端的测试结束时，服务器端反向连接至客户端。当客户端连接终止时，反向连接随即开始。如果需要同时进行双向测试，请尝试 <code>-d</code> 参数</td>
</tr>
</tbody>
</table>
<h3 id="其他参数">其他参数</h3>
<table>
<thead>
<tr>
<th>参数</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>-h, --help</code></td>
<td>显示命令行参考并退出</td>
</tr>
<tr>
<td><code>-v, --version</code></td>
<td>显示版本信息和编译信息并退出</td>
</tr>
</tbody>
</table>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">iperf3 -h
</span></span><span class="line"><span class="cl"><span class="c1"># Usage: iperf3 [-s|-c host] [options]</span>
</span></span><span class="line"><span class="cl"><span class="c1">#        iperf3 [-h|--help] [-v|--version]</span>
</span></span></code></pre></div><h2 id="iperf-使用实例">Iperf 使用实例</h2>
<h3 id="环境准备">环境准备</h3>
<ul>
<li>Server 端 IP 地址：192.168.0.120</li>
<li>Client 端 IP 地址：192.168.0.121</li>
</ul>
<h3 id="测试-tcp-吞吐量">测试 TCP 吞吐量</h3>
<p><strong>（1）Server 端开启 iperf 的服务器模式，指定 TCP 端口</strong></p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="o">[</span>root@iperf-server ~<span class="o">]</span><span class="c1"># iperf3 -s -i 1 -p 520</span>
</span></span><span class="line"><span class="cl">------------------------------------------------------------
</span></span><span class="line"><span class="cl">Server listening on TCP port <span class="m">520</span>
</span></span><span class="line"><span class="cl">TCP window size: 85.3 KByte <span class="o">(</span>default<span class="o">)</span>
</span></span><span class="line"><span class="cl">------------------------------------------------------------
</span></span></code></pre></div><p><strong>（2）Client 端启动 iperf 的客户端模式，连接服务端</strong></p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="o">[</span>root@iperf-client ~<span class="o">]</span><span class="c1"># iperf -c 192.168.0.120 -i 1 -t 60 -p 520</span>
</span></span><span class="line"><span class="cl">------------------------------------------------------------
</span></span><span class="line"><span class="cl">Client connecting to 192.168.0.120, TCP port <span class="m">520</span>
</span></span><span class="line"><span class="cl">TCP window size: 45.0 KByte <span class="o">(</span>default<span class="o">)</span>
</span></span><span class="line"><span class="cl">------------------------------------------------------------
</span></span><span class="line"><span class="cl"><span class="o">[</span>  3<span class="o">]</span> <span class="nb">local</span> 192.168.0.121 port <span class="m">50616</span> connected with 192.168.0.120 port <span class="m">520</span>
</span></span><span class="line"><span class="cl"><span class="o">[</span> ID<span class="o">]</span> Interval       Transfer     Bandwidth
</span></span><span class="line"><span class="cl"><span class="o">[</span>  3<span class="o">]</span>  0.0-10.1 sec  1.27 GBytes  1.08 Gbits/sec
</span></span></code></pre></div><p><strong>（3）Server 端监听结果</strong></p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-text" data-lang="text"><span class="line"><span class="cl">------------------------------------------------------------
</span></span><span class="line"><span class="cl">Server listening on TCP port 5001
</span></span><span class="line"><span class="cl">TCP window size: 85.3 KByte (default)
</span></span><span class="line"><span class="cl">------------------------------------------------------------
</span></span><span class="line"><span class="cl">[  4] local 192.168.0.120 port 520 connected with 192.168.0.121 port 50616
</span></span><span class="line"><span class="cl">[ ID] Interval       Transfer     Bandwidth
</span></span><span class="line"><span class="cl">[  4]  0.0-10.1 sec  1.27 GBytes  1.08 Gbits/sec
</span></span></code></pre></div><p>输出字段说明：</p>
<ul>
<li><strong>Interval</strong>：时间间隔</li>
<li><strong>Transfer</strong>：时间间隔里面传输的数据量</li>
<li><strong>Bandwidth</strong>：时间间隔里的传输速率</li>
</ul>
<p><strong>（4）测试多线程 TCP 吞吐量</strong></p>
<p>如果没有指定发送方式，iPerf 客户端只会使用单线程。</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">iperf3 -c 192.168.0.120 -P <span class="m">30</span> -t <span class="m">60</span>
</span></span></code></pre></div><p><strong>（5）进行上下行带宽测试（双向传输）</strong></p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">iperf3 -c 192.168.0.120 -d -t <span class="m">60</span>
</span></span></code></pre></div><p><strong>（6）其他建议</strong></p>
<ul>
<li>建议在 Server 端执行 <code>sar</code> 命令来统计实际收到的包并作为实际结果：<code>sar -n DEV 1 320</code></li>
<li>要停止 iperf3 服务进程，请按 <code>Ctrl+Z</code> 或 <code>Ctrl+C</code></li>
</ul>
<h3 id="测试-udp-吞吐量">测试 UDP 吞吐量</h3>
<p>带宽测试通常采用 UDP 模式，因为能测出极限带宽、时延抖动、丢包率。在进行测试时，首先以链路理论带宽作为数据发送速率进行测试，例如，从客户端到服务器之间的链路的理论带宽为 100Mbps，先用 <code>-b 100M</code> 进行测试，然后根据测试结果（包括实际带宽、时延抖动和丢包率），再以实际带宽作为数据发送速率进行测试，会发现时延抖动和丢包率比第一次好很多，重复测试几次，就能得出稳定的实际带宽。</p>
<p><strong>（1）Server 端开启 iperf 的服务器模式，指定 UDP 端口</strong></p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="o">[</span>root@iperf-server ~<span class="o">]</span><span class="c1"># iperf3 -s -i 1 -p 521</span>
</span></span><span class="line"><span class="cl">------------------------------------------------------------
</span></span><span class="line"><span class="cl">Server listening on port <span class="m">521</span>
</span></span><span class="line"><span class="cl">------------------------------------------------------------
</span></span></code></pre></div><p><strong>（2）Client 端启动 iperf 的客户端模式，连接服务端</strong></p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="o">[</span>root@iperf-client ~<span class="o">]</span><span class="c1"># iperf3 -u -c 192.168.0.120 -b 100m -t 60 -p 521</span>
</span></span><span class="line"><span class="cl">------------------------------------------------------------
</span></span><span class="line"><span class="cl">Client connecting to 192.168.0.120, port <span class="m">521</span>
</span></span><span class="line"><span class="cl">------------------------------------------------------------
</span></span><span class="line"><span class="cl"><span class="o">[</span>  3<span class="o">]</span> <span class="nb">local</span> 192.168.0.121 port <span class="m">50616</span> connected with 192.168.0.120 port <span class="m">521</span>
</span></span><span class="line"><span class="cl"><span class="o">[</span> ID<span class="o">]</span> Interval       Transfer     Bandwidth       TotalDatagrams
</span></span><span class="line"><span class="cl"><span class="o">[</span>  3<span class="o">]</span>  0.0-10.1 sec  1.27 GBytes  1.08 Gbits/sec  <span class="m">82</span>
</span></span></code></pre></div><p><strong>（3）Server 端监听结果</strong></p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-text" data-lang="text"><span class="line"><span class="cl">------------------------------------------------------------
</span></span><span class="line"><span class="cl">Server listening on port 521
</span></span><span class="line"><span class="cl">------------------------------------------------------------
</span></span><span class="line"><span class="cl">[  4] local 192.168.0.120 port 520 connected with 192.168.0.121 port 50616
</span></span><span class="line"><span class="cl">[ ID] Interval       Transfer     Bandwidth       Jitter   Lost/Total Datagrams
</span></span><span class="line"><span class="cl">[  4]  0.0-10.1 sec  1.27 GBytes  1.08 Gbits/sec  0.007 ms  0/82 (0%)
</span></span></code></pre></div><p>输出字段说明：</p>
<ul>
<li><strong>Jitter</strong>：抖动，在连续传输中的平滑平均值差</li>
<li><strong>Lost</strong>：丢包数量</li>
<li><strong>Total Datagrams</strong>：包数量</li>
</ul>
<p><strong>（4）测试多线程 UDP 吞吐量</strong></p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">iperf3 -u -c 192.168.1.1 -b 5M -P <span class="m">30</span> -t <span class="m">60</span>
</span></span></code></pre></div><p><strong>（5）进行上下行带宽测试（双向传输）</strong></p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">iperf3 -u -c 192.168.1.1 -b 100M -d -t <span class="m">60</span>
</span></span></code></pre></div><h2 id="jperf-介绍">Jperf 介绍</h2>
<h3 id="jperf-简介">Jperf 简介</h3>
<p>jperf 是基于 iperf 开发的图形界面程序，简化了复杂命令行参数的构造，而且还能够保存测试结果，同时实时图形化显示结果。JPerf 可以测试 TCP 和 UDP 带宽质量，可以测量最大 TCP 带宽，具有多种参数和 UDP 特性，可以报告带宽、延迟抖动和数据包丢失。</p>
<h3 id="jperf20-运行环境">JPerf2.0 运行环境</h3>
<ul>
<li><strong>操作系统</strong>：Java 运行环境</li>
<li><strong>网络要求</strong>：Jperf 可以在任何 IP 网络上运行，包括本地以太网、因特网接入连接和 Wi-Fi 网络</li>
<li><strong>其他要求</strong>：JPerf 必须安装两个组件：JPerf 服务器和 JPerf 客户端</li>
</ul>
<h3 id="jperf20-页面介绍">JPerf2.0 页面介绍</h3>
<p><img src="./images/jperf-main.png" alt="JPerf 主界面" title="JPerf Main"></p>
<p><img src="./images/jperf-settings.png" alt="JPerf 设置界面" title="JPerf Settings"></p>
<p><strong>（1）Iperf 命令行（无法直接输入）</strong></p>
<p>服务端设置：</p>
<ul>
<li>监听端口：5001</li>
<li>Client limit：客户端限制，仅允许指定客户端连接</li>
<li>Num Connections：最大允许连接的数量，为 0 不限制</li>
</ul>
<p>客户端设置：</p>
<ul>
<li>服务器地址：10.0.115.82，端口：5001，并发流：1 个</li>
</ul>
<p><strong>（2）应用层设置</strong></p>
<ul>
<li>兼容旧版本（当 server 端和 client 端版本不一样时使用）</li>
<li>设置测试模式：传输字节总量大小 15 Bytes 或者按传输时间总长度 15 秒</li>
<li>同时进行双向传输测试</li>
<li>单独进行双向传输测试，先测 c 到 s 的带宽</li>
<li>指定需要传输的文件</li>
<li>显示 TCP 最大 MTU 值</li>
</ul>
<p><strong>（3）传输层设置</strong></p>
<p>TCP 协议：</p>
<ul>
<li>设置缓冲区大小</li>
<li>指定 TCP 窗口大小</li>
<li>设定 TCP 数据包的最大 MTU 值</li>
<li>设定 TCP 不延时</li>
</ul>
<p>UDP 协议：</p>
<ul>
<li>设置 UDP 最大带宽</li>
<li>设置 UDP 缓冲区</li>
<li>UDP 包封装大小：默认 1470 byte</li>
</ul>
<p>IP 层设置：</p>
<ul>
<li>指定 TTL 值</li>
<li>服务类型（Type of Service，ToS），大小范围从 0x10（最小延迟）到 0x2（最少费用）</li>
</ul>
<h2 id="参考资料">参考资料</h2>
<ol>
<li><a href="https://help.aliyun.com/knowledge_detail/55757.html#HFXbx">《网络性能测试方法》</a> — 阿里云帮助文档</li>
<li><a href="https://baike.baidu.com/item/iperf/11067694?fr=aladdin">《iperf - 百度百科》</a></li>
<li><a href="https://www.cnblogs.com/klb561/p/9215952.html">《Linux 网络性能评估工具 iperf、CHARIOT 测试网络吞吐量》</a> — Konglingbin</li>
<li><a href="https://man.linuxde.net/iperf">《Linux 命令大全 - iperf 命令》</a></li>
<li><a href="http://www.veryhuo.com/a/view/159685.html">《网络性能测试工具 iPerf 功能与使用教程》</a></li>
<li><a href="http://www.veryhuo.com/a/view/159704.html">《iPerf 图形化工具 Jperf 图文使用教程》</a></li>
<li><a href="https://www.jianshu.com/p/15f888309c72">《使用 iPerf 进行网络吞吐量测试》</a> — 放开那个电扇</li>
</ol>
]]></content:encoded>
    </item>
    
    <item>
      <title>About</title>
      <link>https://devcharl.github.io/en/about/</link>
      <pubDate>Sun, 20 Aug 2017 21:38:52 +0800</pubDate>
      
      <guid>https://devcharl.github.io/en/about/</guid>
      <description>About This Site A personal blog for learning notes and development records.
About Me Software developer Interested in efficiency, simplicity, movies, reading, travel, and fitness License Original articles on this site are licensed under CC BY-NC 4.0.</description>
      <content:encoded><![CDATA[<h2 id="about-this-site">About This Site</h2>
<p>A personal blog for learning notes and development records.</p>
<h2 id="about-me">About Me</h2>
<ul>
<li>Software developer</li>
<li>Interested in efficiency, simplicity, movies, reading, travel, and fitness</li>
</ul>
<h2 id="license">License</h2>
<p>Original articles on this site are licensed under <a href="https://creativecommons.org/licenses/by-nc/4.0/">CC BY-NC 4.0</a>.</p>
]]></content:encoded>
    </item>
    
    <item>
      <title>关于</title>
      <link>https://devcharl.github.io/about/</link>
      <pubDate>Sun, 20 Aug 2017 21:38:52 +0800</pubDate>
      
      <guid>https://devcharl.github.io/about/</guid>
      <description>关于网站 个人博客，欢迎交流 关于我 咸鱼一枚 喜欢效率和极简，喜欢看电影、读书、旅行和健身 想在这短短的一生中体验这世间美好的一切 关于版权 本站所有的</description>
      <content:encoded><![CDATA[<h2 id="关于网站">关于网站</h2>
<p><strong>个人博客，欢迎交流</strong></p>
<h2 id="关于我">关于我</h2>
<ul>
<li>咸鱼一枚</li>
<li>喜欢效率和极简，喜欢看电影、读书、旅行和健身</li>
<li>想在这短短的一生中体验这世间美好的一切</li>
</ul>
<h2 id="关于版权">关于版权</h2>
<p>本站所有的原创文章均受 <a href="https://creativecommons.org/licenses/by-nc/4.0/">创作共享 署名-非商业性 4.0 许可协议 / CC BY-NC 4.0</a> 保护。</p>
<p>任何个人及媒体在转载本站原创内容（包含文字、自制图像、摄影作品）时请遵守以下版权要求：</p>
<ul>
<li>注明转载</li>
<li>注明来源为本站首页网址 <a href="https://devcharl.github.io/">devcharl.github.io</a>，或所转内容在本站的完整网址</li>
</ul>
<p>本站图片部分来自互联网。此类图片的原版权所有者可在任何时候、以任何理由要求本站停止使用有关图片，其中包括被本站编辑（比如加注中文说明）过的图片，联系方式见本站首页。</p>
]]></content:encoded>
    </item>
    
  </channel>
</rss>